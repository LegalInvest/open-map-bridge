# OpenMapBridge 产品目标与验收真值

## 文档控制

- 项目：OpenMapBridge（工作代号，正式品牌不得使用奥维商标或造成官方关联误解）
- 版本：V0.6.1 奥维图源二次开发平台＋审计修复实施版
- 状态：Approved / Implementing；用户于 2026-08-28 明确最终结果必须能基于已导入奥维图源进行二次开发
- 产品裁决者：用户
- 当前整合者：本 Codex 主线程
- 更新时间：2026-08-28 19:15（Asia/Shanghai）
- 适用目录：`/Users/assis/Documents/Codex/2026-08-27/open-map-bridge`
- 当前切片：`FIX-BATCH-007 main`；预览缓存已有 TTL/数量/字节 LRU，二维码图片已有 object URL/ZXing 前字节/格式/像素/倍率门。PR #13 已合并为 `873705b`，首次红灯 `33166210759` 保留，最终 CI `33166313733` 全绿；当前只在证据分支回写 main/CI。真实目录/provider/probe/ready、部署与用户验收仍未达到
- 上版：V0.4 奥维兼容双入口导入实施版

<!-- GOAL_CAPSULE_START -->
先完整读取本目录的 `goal.md`、`research.md`、`PROGRESS.md`、`BLOCKED.md` 和 `docs/superpowers/specs/2026-08-27-open-map-bridge-v0-design.md`。产品规范以 `goal.md` 为准，当前事实与阶段以实时证据和 `research.md` 为准；冲突必须记入 `research.md`，不得静默降低规格。

产品意图：让合法持有奥维图源二维码或 `.ovmap` 的用户从“已确认配置”继续到真实、受控、可恢复的图源能力，再由同一 source ID 支撑四期历史影像和行业二次开发。用户已批准可视化/自动化方向，并于 2026-08-28 要求逐一修复全量审计问题、同步问题账本与进度 Markdown。`docs/问题账本.md` 是缺陷范围与阶段真值；不得以单批候选替代完整 backlog。

已批准裁决：二维码图片、摄像头和 `.ovmap` 只输出版本化开放定义；confirmed 不等于 probed/rendered。二次开发只获得 source ID、脱敏元数据、真实能力和本地端点；未完成探测/绑定的源只有 `metadata`。真实网络动作必须先过 URL、解析后 IP、端口、DNS 重绑定、回环/私网/云元数据和凭证边界；用户确认前零外联，探测只请求最少必要内容。

硬约束：clean-room 独立实现；不得复制奥维专有代码、商标或绕过会员/设备绑定；不得内置、记录或传播第三方 token；解析前不联网，确认前不请求图源；未知版本、解压异常、内网目标或不安全 URL 必须 fail closed；“文件已解析”“探测成功”“成功出图”是不同事实；任何成功都要有导入回执和可复现实证。

当前只允许修改本项目目录；禁止清理用户文件、接触生产服务器、部署公网、批量抓取图源或离线下载 20 年整域瓦片。不得把真实秘密写入 fixture、日志、回执、普通 JSON 或前端。可用空间低于 8 GiB 立即停止本机构建、测试、截图和新增缓存；2026-08-28 19:09 最新约 5.2 GiB，低于门禁，本轮只能做小型源码/文档并由 GitHub PR CI 验证。发现越界需求写入 `BLOCKED.md`。

`FIX-BATCH-001/002/003/004/005/006/007` 已进入 GitHub main `873705b`。第七批为未确认预览和 QR 图片建立资源上限并由 CI `33166313733` 验证；它不读取用户图片、不上传原图、不发真实请求、不改变 source lifecycle，也不授予 ready。

第三批完成条件已由 CI `33161851375` 的 128 Vitest＋2 Node、8 workspace 类型检查、生产构建和 4 条 Chrome E2E 验证并进入 main：缺失/错误 Host、恶意 Origin、cross-site、缺失/错误 token、app ID 不符、权限不足、写请求缺 CSRF 和超限均在路由前稳定拒绝且不回显秘密；官方 Web 全旅程继续通过；直连 SDK 必须用独立 app token，服务端权限不能被 manifest 扩大。这不代表真实 probe 或 ready。项目最终完成条件仍是同一 source ID 获得有证据的 `tiles/temporal-catalog`，且至少两个真实日期返回可解码、非空并内容不同的瓦片。
<!-- GOAL_CAPSULE_END -->

## 当前技术修复契约

用户于 2026-08-28 批准逐一修复全量审计问题。修复采用小批次、可回滚、逐项取证的方式；完整清单、优先级、状态、验收和追加记录见 `docs/问题账本.md`。

`FIX-BATCH-005` 关联 `OMB-AUD-022`，保护 JRN-007/009/011、BR-011/016/017、FR-009/012/014/016、IF-006/007 和 AC-011/014/015/018。完成候选必须满足：`YYYY-MM-DD` 是实际 UTC 日历日且 from≤to；AOI/date ID 为 1–160 字符、无首尾空白/控制符；路径坐标只接受规范十进制安全非负整数，z≤30 且 x/y<2^z；旧 API、V1、SDK 与适配器复用同一 schema；错误码稳定且无效输入不触发适配器 fetch。HTTP 层可以在进入 schema 前对超长路径以 414 更早 fail closed；不得为统一错误码放宽服务器路径参数门。

`FIX-BATCH-006` 关联 `OMB-AUD-010`，保护 JRN-002、BR-005、FR-002/004、IF-001 和 NFR-004。完成候选必须满足：1 MiB 原始 `.ovmap` 可越过 HTTP 信封门；base64 长度按 `4*ceil(bytes/3)` 计算；仅导入路由增加受限信封预算；1 MiB＋1 字节和超信封分别返回稳定 413；前端在读取/编码/fetch 前拒绝超限；其他 API body limit 不放宽。

`FIX-BATCH-007` 关联 `OMB-AUD-018`，保护 JRN-001/002、BR-004/005、FR-001/002/004 和 NFR-004/006。完成候选必须满足：预览主动 TTL 清理、最多 64 条且总估算 4 MiB 的 LRU；单预览超预算稳定失败；QR 图片声明/实际字节≤8 MiB；在 object URL/ZXing 前从 PNG/JPEG/WebP 头验证尺寸与≤16,777,216 像素；浏览器尺寸与头一致；2×/3× 只在缩放后像素预算内运行；拒绝路径不调用解码器。

## 一页产品定义

### 一句话产品

OpenMapBridge 是一个开源、透明、可扩展的地图工作台：用户可以兼容接入合法奥维图源或标准地图协议，在任意框选区域自动取得跨 20 年的四期对齐影像，并把不透明图源配置转化为可检查、可运行、可迁移的开放定义。

### 目标用户

1. 已经使用奥维、合法持有图源二维码或 `.ovmap`，希望跨设备使用、核验或迁移图源的个人用户。
2. 林业、测绘、工程、无人机、通信、电力等需要在线影像或自有航拍底图的现场人员和小团队。
3. 希望基于开放地图内核开发行业插件、数据处理和协作能力的开发者。

V0 的直接服务角色是“图源导入者”和“插件/行业应用开发者”。二者通过已确认图源和能力目录交接；开发者旅程不得给导入者增加编程步骤。

### 核心问题

- 二维码和 `.ovmap` 内容对普通用户不透明，导入成功不等于图源有效、授权合法或能够出图。
- 互联网流传的配置可能包含过期服务器、共享 token、盗链、错误投影或只适用于特定奥维版本的私有布局。
- 二维码、文件和标准地图服务入口各自为政，难以迁移、审计、扩展或二次开发。
- 现有开源候选只能解决切片、格式转换、简单地图 UI 或官方 IPC 的局部问题，没有一个经过核验的仓库完成本 V0 旅程。

### 价值闭环

用户提供合法配置 → 系统离线解析 → 用户看清事实与风险 → 明确确认 → 受控探测 → 真实出图 → 保存开放定义与回执 → 后续可以稳定复用、分享或扩展。

### 可视化与自动化北极星

用户于 2026-08-28 提出“数字化之后，就是可视化和自动化”。本项目据此采用三层闭环：先把源、日期、区域、能力、状态和证据数字化；再把进度、地图、时间、质量、风险与下一动作可视化；最后让系统自动完成低风险、可恢复步骤，只在授权、秘密、内网风险、冲突和低置信例外时请求人。

该方向不得改变导入优先级或制造假绿。用户已于 2026-08-28 批准实施；第一阶段驾驶舱直接消费真实 confirmed source 的 `AutomationRun/AutomationStep`。当前只覆盖零外联静态准备度，真实探测、绑定、一键四期、续跑、质量卡和来源链仍须分别晋级。完整路线见 `docs/可视化与自动化路线图.md`。

### V0 成功结果

用户不需要手抄 URL 或理解私有二进制，就能判断“这个配置是什么、请求谁、缺什么、是否成功出图”；系统绝不把仅解析成功、仅 HTTP 成功或仅页面出现冒充为可用地图。

### 核心不变量

1. 用户有权使用图源是输入前提，兼容读取不等于平台为来源背书。
2. 未知、未探测、探测失败、渲染失败必须分开，未知不得显示为成功或 0。
3. token、密钥和私有请求头默认不回显、不写日志、不进入导出文件。
4. 外部配置在进入稳定内核前必须经过版本适配、安全检查和归一化。
5. 开放内部格式是长期真值；奥维兼容是边界适配器，不能反向绑死内核。
6. 先保障真实可运行与可恢复，再增加界面相似度和长尾功能。

### 让步顺序

正确性与凭证安全 > 图源授权与来源透明 > 可恢复性 > 兼容覆盖 > 首屏速度 > 视觉还原度。

## 问题证据与当前办法

### 已知事实

- 用户明确要求开源版可扫描真实图源二维码，并导入互联网中的 `.ovmap`。
- 用户接受 Web UI，首要价值是“能跑起来、拉起来”。
- 奥维官方把二维码和 `.ovmap` 都作为自定义地图配置分享入口；本地航拍图迁移还需要配套 `.sdb`。
- 当前抽查的公开 `.ovmap` 样本以 `OviO` 开头，私有头部后存在 zlib 数据，解压可见主机和瓦片 URL 模板；单文件可包含多个图层。
- 工作区没有发现现成的 OpenMapBridge/Ovital 兼容项目，当前为绿地项目。

### 用户当前绕行

用户在奥维中直接扫码或导入文件，只有在选择图层后才能发现是否空白；遇到 token、投影、版本或服务失效时，原因不透明。互联网卖家和社区常以二维码或文件传播配置，来源、授权和稳定性不一致。

### 待验证假设

- 假设 A：用户能提供至少一张有权使用、当前可用的真实二维码用于最终验收。若错误，V0 只能用公开官方模板做结构验证，不能完成目标用户真实源签收。
- 假设 B：V0 优先覆盖当前常见 `OviO + zlib` `.ovmap`，再按合法样本扩展历史版本。若格式家族差异大，兼容矩阵会增长，但不会改变统一模型。
- 假设 C：Web UI 配本地网关可以满足首版移动摄像头和桌面文件导入。若特定浏览器摄像头能力不足，提供二维码图片上传作为等价入口，原生壳后续补齐。

## 角色、权限与词汇

| 角色 | 目标 | V0 权限 | 不具备的权限 |
|---|---|---|---|
| 图源导入者 | 把合法二维码/文件变为可用地图 | 本地解析、查看预览、确认探测、保存、停用、导出开放配置 | 无权绕过源站授权、读取被打码凭证、批量抓取 |
| 本地网关操作员 | 启停本地服务、查看健康和脱敏日志 | 管理本机配置与缓存上限 | 不自动获得第三方 token 明文或生产权限 |
| 外部图源服务器 | 返回用户有权访问的瓦片 | 仅收到用户确认后生成的必要请求 | 不应收到本地文件内容、其他图源凭证或收藏数据 |
| 插件/行业应用开发者 | 基于已导入奥维或标准图源扩展行业能力 | 读取脱敏源目录；按声明能力调用本地日期/瓦片 API；使用版本化 SDK | 不读取原始 QR/`.ovmap`、上游模板或凭证；V0 不在平台内执行任意插件代码 |

关键词：

- **已解析**：输入结构可读，尚未证明网络可达或地图可用。
- **已探测**：一次受控测试请求取得可解释结果，尚未证明整张地图投影与渲染正确。
- **已渲染**：真实地图画布显示来自目标图源的有效瓦片。
- **已保存**：开放定义和脱敏回执已持久化，重启后可恢复。
- **`.ovmap`**：奥维自定义地图配置容器；通常不包含全部在线影像。
- **`.sdb`**：奥维导出的离线地图瓦片数据；V0 识别依赖但不承诺完整导入。
- **地图源**：产生瓦片的外部或本地服务定义。
- **导入回执**：记录输入哈希、解析器版本、用户裁决、探测和渲染结果的脱敏事实。
- **能力描述**：某个 source ID 当前真实可供二次开发调用的最小权限集合；未声明即不可调用。

## 范围与产品边界

### V0 包含

- 响应式 Web UI 与本地网关。
- 通过官方奥维本机 Web 瓦片服务兼容用户已授权的历史影像图源；若无法安全限制在回环接口，真实桥接 fail closed。
- 时序源开放适配器、最近 20 个完整自然年窗口、唯一四期自动选择、请求日期与真实拍摄日期分离。
- 任意矩形/多边形框选、网关验证、确认和版本追溯；宝应湖和高邮湖仅为预设样例。
- 单屏/双屏/四屏同步对比、双屏卷帘、时间轴播放、逐屏加载与错误事实。
- 摄像头扫码、二维码图片上传、`.ovmap` 文件上传/拖入。
- 奥维二维码公开格式适配与版本化 `.ovmap` 解析框架。
- 一个 `.ovmap` 内多图源的逐项预览和选择。
- XYZ、TMS、WMTS、WMS、ArcGIS REST 等归一化模型；V0 优先完成解析实际样本所需协议。
- 安全预览、token 打码、地图 ID 冲突处理、缺 `.sdb` 诊断。
- 用户确认后的一次必要探测、瓦片代理、二维地图真实渲染。
- 本地持久保存、停用和开放格式/二维码导出。
- 导入回执、错误分类和最小健康信息。
- 版本化、只读、脱敏的开发者源目录，以及 TypeScript SDK、插件清单校验和最小消费示例。

### 后续范围

- `.sdb`、MBTiles、PMTiles、GeoTIFF 等离线数据导入和区域缓存。
- 标签、轨迹、图形、照片、附件和 OVOBJ/OVKML/OVJSN 对象系统。
- 企业组织、权限、同步、共享、审计和私有地图服务。
- 云掩膜、跨传感器辐射一致化、自动变化检测、污染因果模型和批量视频渲染队列。
- 三维、CAD、无人机行业成品，以及插件市场、沙箱执行与在线安装。
- Tauri/原生移动壳，但复用当前内核和 schema。

### V0 明确不做

- 不复刻奥维商标、Logo、会员系统或受保护 UI。
- 不破解设备绑定、加密文件或会员限制。
- 不提供盗链、共享 token、去水印或批量离线抓取。
- 不承诺所有历史 `.ovmap` 在没有合法样本时都能正确解码。
- 不把公开仓库中的图源地址自动内置为可用商业服务。
- 不连接用户企业服务器、云服务器或 GitHub 执行部署。
- 不解密或逆向绕过 GEE 私有认证，不把本机奥维瓦片服务暴露到局域网或公网。
- 不把水色、纹理或岸线变化自动写成污染、过度养殖或过度开发的确定因果结论。
- 不因缺少某年影像而插值、复制邻年帧或把请求日期冒充真实拍摄日期。
- 不把开发者 API 变成任意上游 URL 代理，不向插件暴露原始导入内容或密钥库引用。

## 完整用户旅程

### JRN-001 首次扫描二维码并真实出图

- 目标：导入一张有权使用的真实奥维图源二维码并看到地图。
- 入口：地图首页“导入图源”→“摄像头扫码”或“上传二维码图片”。
- 前置：本地网关健康；浏览器具备摄像头权限或用户有二维码图片；用户对图源具有使用权。
- 主流程：选择入口 → 系统只在本地解码 → 显示地图名、域名、协议、投影、级别、token 是否存在及风险 → 用户勾选授权确认 → 点击“测试并导入” → 系统发出最少必要请求 → 展示探测结果 → 地图画布显示真实瓦片 → 保存并生成回执。
- 异常：拒绝摄像头权限时转为图片上传；二维码无法识别时允许重拍/更换文件；token 为占位符时停在“需要凭证”；服务器 401/403/404/429/超时分别解释并可编辑后重试；投影未知时不静默猜测。
- 完成：图层状态为“已渲染、已保存”，显示来源域名和最后验证时间；后续动作是继续浏览或查看导入回执。
- 验收：AC-001、AC-004、AC-005、AC-006、AC-007。

### JRN-002 导入包含多个图层的 `.ovmap`

- 目标：从一个文件选择性导入多个在线地图配置。
- 入口：“导入图源”→“选择 `.ovmap`”或拖入文件。
- 前置：文件满足大小限制；扩展名不是信任依据。
- 主流程：读取魔数和头部 → 有界解压 → 按版本解析记录 → 显示每个图层的名称、原 ID、主机、URL 脱敏摘要、投影和兼容状态 → 用户逐项勾选 → 冲突 ID 自动建议新本地 ID → 用户确认 → 对选中项逐个探测，允许部分成功 → 保存成功项并为失败项保留独立错误。
- 异常：损坏、未知版本、解压比例异常或字段越界时停止网络操作；包含本地航拍语义但缺 `.sdb` 时标记“需要配套离线数据”；一项失败不得回滚其他已确认成功项，但用户可一键撤销本次批次。
- 完成：每个图层都有独立状态和回执，批次摘要区分成功、需要凭证、需要数据、失败和未选择。
- 验收：AC-002、AC-003、AC-008、AC-009。

### JRN-003 日常回访和切换图层

- 目标：重启后快速继续使用已导入图源。
- 入口：地图首页图层面板。
- 主流程：看到保存图层及最后验证状态 → 选择图层 → 地图通过本地网关请求瓦片 → 缓存命中时直接返回 → 状态过期或失败时显示原因和“重新验证”。
- 异常：源站失效不删除配置；token 失效进入“需要凭证”；网络离线显示缓存范围和离线状态，不把空白当成功。
- 完成：用户继续浏览，或明确知道为何暂不可用。
- 验收：AC-006、AC-007。

### JRN-004 错误诊断与恢复

- 目标：从失败原因恢复，而不是反复盲扫。
- 入口：导入预览、探测结果、图层错误状态或导入回执。
- 主流程：查看错误类别和脱敏证据 → 根据建议更换 token、修改投影/协议、选择配套文件、稍后重试或放弃 → 仅重试失败步骤 → 成功后追加新回执，不覆盖历史失败事实。
- 异常：连续失败三次后停止自动重试；原文件和配置不被破坏；用户可导出脱敏诊断。
- 完成：恢复为已渲染，或明确停在不可支持/缺授权状态。
- 验收：AC-003、AC-004、AC-008。

### JRN-005 安全受限与人工放行

- 目标：阻止恶意或高风险配置，同时允许用户明确配置合法内网企业源。
- 入口：预览中的安全警告。
- 主流程：系统识别 HTTP、裸 IP、localhost、私网地址、危险端口、超长 URL、文件协议和凭证泄漏 → 默认阻止 → 只对明确的本地企业源提供二次确认和按主机授权 → 记录脱敏裁决。
- 异常：浏览器和网关策略不一致时以后端拒绝为准；禁止通过重命名文件绕过。
- 完成：安全源被允许，危险源保持阻止且配置未进入活动状态。
- 验收：AC-004、AC-009。

### JRN-006 导出开放配置并继续使用

- 目标：避免用户再次被封闭格式锁定。
- 入口：已保存图层“导出”。
- 主流程：选择导出为 `.oms.json` 或开放二维码 → 默认移除 token 和私有请求头 → 显示包含/不包含内容 → 用户保存 → 重新导入可恢复非秘密配置。
- 异常：图源离不开凭证时提示接收者需自行配置；未知原始字段只进入可选兼容附件，不能伪装为开放标准字段。
- 完成：生成可读、版本化、带来源说明的开放文件或二维码。
- 验收：AC-010（后置 V0 验收，不阻塞首次出图内测；正式 V0 发布前必须通过）。

### JRN-007 首次打开通用四期历史影像

- 目标：不用先理解图源协议，在任意地图位置框选区域并得到跨 20 年的四期真实影像。
- 入口：首页“历史影像四期对比”→选择图源→“新建框选区域”。
- 前置：本地网关健康；官方奥维已导入用户有权使用的历史源；桥接只允许本机访问。
- 主流程：用户选择矩形或多边形并命名 → 网关验证并保存区域 → 系统计算最近 20 个完整自然年 → 获取日期目录并区分可用、缺失、未知和失败 → 自动选择最多四个唯一日期 → 四屏加载同一 AOI → 每屏显示请求日期、拍摄日期或未知、加载瓦片数和来源。
- 异常：桥接未启用、日期目录不可列、某年无数据、单屏超时/429、影像为空分别显示可恢复下一步；其他面板不被重置。
- 完成：至少两个历史日期的真实非空瓦片可切换；后续动作是编辑/确认 AOI 或开始对比。
- 验收：AC-011、AC-012、AC-013。

### JRN-008 创建或修订任意区域

- 目标：把用户在真实地图上的框选保存为可复用空间真值，也能继续修订双湖等预设。
- 入口：历史影像左栏“新建框选区域”或既有区域“编辑范围”。
- 主流程：用户选择矩形/多边形并命名 → 查看底图位置 → 点击“使用此范围” → 网关生成 confirmed 版本 1；编辑既有区域时确认后追加新版本，不覆盖旧版本 → 所有面板切换到新版本。
- 异常：自交、多边形为空、面积异常、超出合理区域或保存失败时保持草稿并说明；取消恢复上次已确认版本。
- 完成：AOI 标签变为 confirmed，比较回执冻结该版本。
- 验收：AC-012、AC-013、AC-014。

### JRN-009 四屏、卷帘与时间轴观察变化

- 目标：在严格对齐的画布上检查 20 年变化，而不是来回切图凭记忆判断。
- 入口：已选择 AOI 和日期目录的历史影像工作台。
- 主流程：选择 1/2/4 屏 → 每屏选日期 → 平移/缩放/旋转任一屏并同步 → 可切换双屏卷帘 → 点击播放并调速度/暂停/前后帧 → 对可见变化添加观察记录和证据等级。
- 异常：帧缺失时显示缺口并继续到下一可用帧；一屏失败保持独立错误；用户可重试该屏；播放暂停后从当前帧恢复。
- 完成：用户能回看帧回执并区分“可见变化”和“原因假设”。
- 验收：AC-014、AC-015、AC-016。

### JRN-010 回访并继续上次比较

- 目标：刷新或重启后继续任意已保存区域的比较，不重新框选日期和范围。
- 入口：历史影像首页“最近比较”。
- 主流程：恢复源 ID、AOI 版本、四个日期、ViewState 和模式 → 重新请求可见瓦片 → 新加载事实追加到回执。
- 异常：源失效或 AOI 已有新版本时保留旧比较只读，并提示创建新版比较；不得静默改用新边界。
- 完成：恢复到同一空间和日期上下文，或明确知道缺失依赖。
- 验收：AC-014、AC-015。

### JRN-011 基于已导入奥维图源开发行业应用

- 目标：开发者不用理解或复制奥维私有格式，即可把用户已授权、已导入的图源接入历史影像或其他行业应用。
- 入口：本地开发者文档、`@omb/developer-sdk` 和 `/api/v1/developer/sources`。
- 前置：本地网关健康；用户已确认至少一个图源；应用清单声明 API 版本、所需能力和只读权限。
- 主流程：校验应用清单 → 通过 SDK 列出脱敏源目录 → 选择 source ID → 检查能力 → 有 `temporal-catalog` 时列日期 → 有 `tiles` 时只取得本地网关瓦片模板 → 在应用中消费结果。
- 分支：已确认但尚未绑定运行时适配器的奥维源只返回 `metadata`；合成或已就绪适配器按真实能力返回日期与瓦片端点。
- 异常与恢复：源不存在返回稳定 404；能力缺失在 SDK 发网前拒绝；网关错误保留稳定代码；API 主版本不兼容时停止而不猜测；源失效后保留元数据并撤销运行能力。
- 完成：示例应用使用 source ID 完成一次能力发现与允许的调用，全程没有取得上游地址、凭证或私有字节；后续动作是接入具体行业 UI 或等待真实源绑定。
- 验收：AC-017、AC-018。

### JRN-012 一键生成可追溯四期比较（已批准；阶段实施）

- 目标：用户完成授权、选择图源和框选区域后，不再手工串联探测、日期、选四期、加载和重试，而由系统自动推进并持续显示真实状态。
- 入口：已确认图源的“继续验证并生成四期”，或历史工作台“一键生成四期”。
- 前置：source ID、AOI 版本和本地网关可用；需要凭证或企业内网授权时允许停在人工门。
- 主流程：创建任务 → 安全校验 → 必要人工门 → 最小探测 → 运行时绑定 → 日期目录 → 可解释四期选择 → 最少瓦片加载/质检 → 冻结比较与回执 → 展示四屏/卷帘/时间轴和来源链。
- 异常与恢复：403/429/超时、单帧空白、网关重启或重复点击都保留已完成步骤，只重试可重试步骤；不得重复外联、重复保存或把 partial 写成 completed。
- 完成：用户看到任务每一步、耗时、输入输出摘要、错误和唯一下一动作；成功时得到最多四个真实唯一帧，失败时准确知道停在哪里。
- 验收：AC-019 至 AC-022。

## 信息架构、页面与动作

### UI-001 地图首页

- 目的：浏览地图并进入图源管理。
- 可见事实：当前图层、来源域名、状态（未验证/已解析/已探测/已渲染/失效/需要凭证/需要数据）、最后验证时间；未知显示“未知”。
- 首要动作：没有图层时“导入图源”；已有图层时“选择图层”。
- 次要动作：重新验证、停用、查看回执、导出。
- 状态：初始引导、加载、无图层、正常、部分瓦片失败、离线、网关不可达、图源失效和无权限。
- 移动/桌面：业务动作等价；移动端导入入口优先显示摄像头，桌面优先显示拖入和二维码图片。

### UI-002 导入入口

- 标签页：摄像头扫码、二维码图片、`.ovmap` 文件、手动标准服务。
- 选择输入后立即显示本地解析进度；此阶段不得请求图源服务器。
- 摄像头拒权、无摄像头、文件过大、扩展名错误但魔数正确、魔数错误分别给出下一步。
- 用户取消即清除未确认的临时解析结果和内存中的敏感值。

### UI-003 安全预览与选择

- 显示：输入哈希短码、解析器版本、图层列表、协议、域名、端口、投影、级别、图片格式、token 存在性、授权声明、风险和兼容状态。
- token、密钥和敏感参数只显示是否存在及末尾最多四位；复制默认禁用。
- 每个图层可勾选；不兼容项不可勾选并显示原因；地图 ID 冲突展示建议的新本地 ID。
- 首要动作：“测试并导入”；前置是至少选中一项并确认有权使用。
- 进行中显示逐项进度；取消停止未发出的请求，已完成项进入待确认摘要。

### UI-004 导入结果与回执

- 批次摘要区分：已渲染、已保存、需要凭证、需要 `.sdb`、服务器拒绝、格式不支持、用户未选择。
- 每项展示一次测试请求的时间、结果类别和脱敏主机，不展示完整 token。
- 动作：打开地图、编辑配置、重试失败项、撤销本批次、导出脱敏诊断。
- 撤销只删除本批次新建的本地定义和相关小型缓存，不影响用户此前图层或源站数据。

### UI-005 历史影像四期工作台

- 顶部显示区域、动态 20 年时间范围、图源阶段、日期精度、四屏/卷帘模式和播放动作。
- 左侧显示“新建框选区域”、已保存 AOI、approximate/confirmed、编辑/取消/确认范围和真实日期目录。
- 主画布默认四屏；每屏独立日期，统一 ViewState，并显示请求日期、拍摄日期或未知、来源、loaded/partial/missing/failed 和瓦片计数。
- 加载、部分成功、单屏失败、全部失败、桥接不可达、日期目录未知、离线和恢复均有独立状态；不能只用颜色表达。
- 首要动作由当前状态决定：AOI 未确认时“确认范围”，日期已就绪时“开始对比”，已有比较时“播放变化”。

### UI-006 AOI 创建、编辑与变化观察

- 新建区域支持矩形/多边形绘制、名称、取消和“使用此范围”；网关返回 confirmed 版本 1。AOI 编辑展示顶点、面积、版本、来源和确认时间；保存会创建新版本，取消恢复上次确认版本。
- 观察记录包含位置、起止日期、可见对象、变化方向、关联帧和证据等级。
- “污染”“过度养殖”“过度开发”默认只能作为假设标签；没有外部证据时必须显示“尚不能由影像单独证明”。

### UI-007 开发者调用契约

- V0 以文档、SDK 和 JSON API 为主要界面，不新增插件市场页面。
- 调用方先读取源目录中的状态、能力、日期精度和本地链接；未声明能力不显示为可用，也不能靠构造 URL 绕过。
- `metadata-only`、`ready`、源不存在、能力不足、版本不兼容和网关不可达均有稳定错误与恢复说明。
- 示例不得要求复制真实 token、上游 URL 或奥维原始载荷；只使用 source ID 和本地 API。

### UI-008 自动任务驾驶舱（已批准；准备度切片 main）

- 首页用“解析→授权→安全→探测→绑定→日期→选四期→加载质检→比较/导出”展示任务，而不是把状态散落在多个页面。
- 每步显示状态、开始/结束、耗时、是否外联、尝试次数、错误码、回执和下一动作；未知必须显示未知。
- 当前运行有一个首要动作；低风险步骤自动继续，人工门只用于授权、秘密、内网风险、AOI 冲突和低置信选择。
- 支持取消、恢复、只重试失败步骤和查看来源链；刷新/重启后可恢复。
- 当前 V1 子集只展示“图源确认→网络静态策略→凭证准备→运行时绑定”四步、每步是否外联和唯一下一动作；取消/续跑、真实探测及后续五步尚未实现。

## 业务规则与状态机

### BR-001 事实状态分离

地图源状态机为：`received → parsed → confirmed → probed → rendered → saved`，并允许从任一网络阶段进入 `needs-credential / needs-data / blocked / failed / stale`。后续成功追加事实，不抹去历史失败。

### BR-002 不以扩展名判断格式

`.ovmap` 必须验证魔数、头部边界、压缩流和记录结构；二维码图片必须先成功解码出载荷，再识别协议。扩展名只用于入口提示。

### BR-003 有界解压

解析器必须限制输入尺寸、解压后尺寸、解压比例、记录数、字符串长度和嵌套深度。任何越界在联网前失败。

### BR-004 凭证规则

token、API Key、Cookie、Authorization 及自定义敏感请求头不得进入前端日志、URL历史、导入回执或默认导出。存储只保留密钥引用；用户删除图源时同步删除对应本地凭证。

### BR-005 用户确认前零外联

解析和预览阶段不对配置中的任何主机发出 DNS、HTTP、图片预加载或追踪请求。用户确认后才允许受控探测。

### BR-006 最小探测

每个图源默认只请求证明协议和内容所需的最少瓦片；不得自动遍历区域或层级。429 后尊重重试提示，不自动并发轰炸。

### BR-007 地图 ID 冲突

保留原始奥维 ID 作为 `legacyId`，平台使用独立稳定 UUID。冲突时建议新的显示编号，不覆盖既有图层，除非用户明确选择替换且可撤销。

### BR-008 多图层部分成功

一个 `.ovmap` 内各图层独立解析、确认、探测和保存。批次结果不得因一项失败而写成全部失败或全部成功。

### BR-009 来源与授权

平台记录用户声明、来源 URL 或文件来源说明和版权归属字段；“可访问”不等于“有授权”。未提供来源时显示未知，不自动推断为公共领域。

### BR-010 兼容与开放内核

QR、`.ovmap` 和标准协议适配器只能输出版本化 `MapSourceDefinition`；渲染器和后续插件不得依赖奥维私有字节布局。未知字段可作为隔离的原始扩展保留，不能改变核心字段含义。

### BR-011 时间事实分离

`requestDate`、`captureDate` 和 `availability` 是不同事实。源接口无法返回拍摄日期时 `captureDate=null`，UI 显示未知；缺年不得复制邻年、插值或伪造。

### BR-012 空间对齐

一次比较冻结源、AOI 版本、投影、中心、缩放、旋转和面板尺寸。四屏使用同一 ViewState；源影像自身正射误差只能标记风险，不能通过随意拉伸制造伪对齐。

### BR-013 AOI 版本

截图轮廓只产生 `approximate` 初始版本。用户在真实地图确认后才进入 `confirmed`；每次修改追加版本，既有回执永远引用原版本。

### BR-014 变化与原因

影像可以证明“在某位置、两个日期之间可见对象发生变化”；污染、渔猎、养殖和开发原因属于解释。没有水质、养殖范围、土地利用或政策等独立证据时，不得升级为确定结论。

### BR-015 本机桥接

OviBridge 只调用官方客户端已经授权的本机接口，不解密私有认证。监听不能限制回环地址时保持禁用；网关不得接受前端传入任意 URL、host 或 token。

### BR-016 通用 20 年四期

默认窗口为当前 UTC 年之前的 20 个完整自然年。四个锚点含首尾并等距分布；每个锚点只能选择一个未使用候选，`available` 优先、`unknown` 可请求、`missing/failed` 排除。候选不足四个时显示真实期数，不复制日期。

### BR-017 二次开发能力最小授权

每个源公开 `metadata`、`temporal-catalog`、`tiles` 中当前真实可用的集合。已确认但未完成运行时绑定或探测的源只能公开 `metadata`；插件调用未声明能力必须在产生网络请求前失败。状态变化只能缩减或增加有证据支持的能力，不能由名称、协议或二维码类型推断。

### BR-018 私有边界与版本

二次开发契约只公开 source ID、显示名、协议/投影等非秘密元数据、能力、日期精度和本地网关链接。原始二维码、`.ovmap` 字节、输入哈希、上游 hosts/path/query、兼容私有扩展和 `credentialRef` 永不进入开发者描述。V1 插件清单只允许已知只读权限；未知 API 版本、能力或权限 fail closed。

### BR-019 自动化分级与人工门（已批准；阶段实施）

V0/V1 自动化目标为 L3“一次触发、安全跑完整闭环”，不追求未经治理的无人值守批量 L4。授权、秘密输入、合法内网放行、AOI 冲突、低置信日期选择和因果结论升级必须人工裁决；其他可重试低风险步骤默认自动。

### BR-020 幂等、续跑与状态真值（已批准；去重/持久化 main）

任务和步骤使用稳定幂等键；刷新、重启和重复点击不得重复外部探测或业务写入。任务状态由持久业务事件推导，前端不得从日志或按钮状态猜测；`partial/blocked/unknown` 不得显示为完成。

## 功能规格

### FR-001 二维码输入与解码

图源导入者从摄像头或图片取得二维码载荷；系统本地解码，识别奥维兼容载荷和开放二维码，无法识别时保留错误类别而非原始敏感全文。连接 JRN-001、UI-002、BR-004/005、AC-001/004。

### FR-002 `.ovmap` 版本化解析

系统嗅探 `OviO`、读取头部、执行有界解压并用显式版本适配器解析单/多图层；未知版本返回结构化不支持结果并保留哈希。连接 JRN-002、BR-002/003/008、AC-002/003。

### FR-003 统一地图源模型

所有入口归一化为同一 schema，区分原始事实、推断字段和用户修正；schema 可版本迁移。连接全部主旅程、BR-010、AC-001/002/006。

### FR-004 安全预览与授权确认

系统在联网前展示可验证字段和风险，遮蔽秘密，并要求用户确认使用权。后端重复执行安全策略，不能只依赖前端。连接 JRN-001/002/005、UI-003、AC-004/009。

### FR-005 受控瓦片代理与渲染

本地网关替换 z/x/y、子域名、时间和非秘密参数，请求最少瓦片，验证响应类型并交给二维地图显示；处理 CORS、超时、限流和缓存。连接 JRN-001/003/004、AC-005/007。

### FR-006 本地持久化与恢复

保存开放定义、凭证引用、状态和回执；刷新/重启后恢复。删除、停用、撤销互不混淆。连接 JRN-003、UI-001/004、AC-006。

### FR-007 诊断与导入回执

为每批和每图层生成脱敏回执，记录输入哈希、解析器版本、用户确认、探测时间、结果类别和渲染结果；支持失败重试和脱敏导出。连接 JRN-002/004、AC-003/004/008。

### FR-008 开放导出

将已保存定义导出为版本化 `.oms.json` 和开放二维码；默认不含秘密，可重新导入恢复非秘密字段。连接 JRN-006、BR-004/010、AC-010。

### FR-009 时序源适配与奥维桥接

以 `TemporalSourceAdapter` 统一 probe、日期目录和按日期瓦片；首个实现通过官方奥维本机接口访问已导入的历史源，不复现私有认证。连接 JRN-007、BR-011/015、AC-011。

### FR-010 任意 AOI 创建、编辑和版本化

用户在地图绘制矩形或多边形并命名，网关验证后生成 confirmed 版本 1；编辑既有区域时追加版本化 EPSG:4326 GeoJSON。宝应湖、高邮湖 approximate 预设只用于示例；比较和回执引用不可变版本。连接 JRN-008、BR-012/013、AC-012/013/014。

### FR-011 同步多屏与卷帘

提供 1/2/4 屏和双屏卷帘；所有地图共享可序列化 ViewState，每屏独立日期和加载事实，事件同步不能循环。连接 JRN-007/009、BR-011/012、AC-012/013/014。

### FR-012 时间轴播放和帧回执

按可用日期顺序播放、暂停、前后帧和调速；帧只有在真实非空瓦片加载后成为 loaded，缺失/失败不伪装成功。连接 JRN-009/010、AC-015。

### FR-014 最近 20 年四期自动选择

系统按 BR-016 计算动态窗口和唯一四期。真实源只提供请求日目录时允许 `unknown` 候选进入请求，但加载失败仍按屏记录；不得把请求日期冒充拍摄日期。连接 JRN-007/009、AC-012/014。

### FR-013 变化观察与证据等级

允许用户记录可见变化，并把原因解释标为假设或有外部证据支持；没有外部数据不得生成确定污染/过度开发结论。连接 JRN-009、BR-014、AC-016。

### FR-015 脱敏源目录与能力网关

网关合并已确认导入源和已注册运行时适配器，输出版本化开发者描述；按 capability 暴露日期和瓦片路由。日期和瓦片请求仍只接受 source ID 与有界参数，禁止客户端指定上游 URL、host、请求头或 token。连接 JRN-011、UI-007、BR-017/018、AC-017/018。

### FR-016 TypeScript SDK 与应用清单

SDK 提供源列表、单源读取、能力断言、日期读取和本地瓦片 URL 构造；应用清单声明 V1、所需能力与只读权限并经过严格校验。能力不足时 SDK 在调用 fetch 前抛出稳定错误，示例应用只能依赖公开契约。连接 JRN-011、BR-017/018、AC-017/018。

### FR-017 可观察任务编排（已批准；准备度子集 main）

网关持久化 `AutomationRun/AutomationStep/InterventionRequest`，串联安全策略、探测、运行时绑定、日期目录、选四期、加载质检和比较回执。每个步骤产生结构化事件，支持取消、恢复和只重试失败步骤。连接 JRN-012、UI-008、BR-019/020、AC-019/021。

当前 V1 子集只执行已确认状态、零外联主机/路径策略、脱敏凭证需求和 registry 绑定判定；四步一次性求值并原子保存。它没有发出 DNS/HTTP 请求，也没有实现步骤级续跑、取消或结果资源。

### FR-018 可解释四期与结果事实层（已批准；尚未实现）

在现有等距唯一选择基线上加入可用性、日期可信度、AOI 覆盖、内容差异和已知质量元数据评分；未知不按 0 处理。结果页展示选择理由、帧质量、来源链和变化候选，不能自动生成无证据因果。连接 JRN-012、BR-011/014/020、AC-020/022。

## 操作与运营说明

### OPS-001 首次使用

启动本地网关 → 打开 Web UI → 检查“本地服务正常” → 点击“导入图源” → 选择扫码、图片或 `.ovmap` → 阅读预览 → 确认授权 → 测试并导入 → 在地图首页验证真实瓦片和回执。

### OPS-002 日常使用

打开首页 → 选择已保存图层 → 查看来源和最后验证状态 → 浏览；空白时先看状态，不反复导入同一文件。

### OPS-003 异常恢复

需要凭证时只在本地安全表单填写；服务器拒绝时查看状态码类别；投影未知时选择明确参数后重新验证；缺 `.sdb` 时保存配置但保持非活动，等待后续离线数据导入能力。

### OPS-004 管理与审计

用户可停用、重新验证、导出开放配置、撤销最近批次和删除本地图源。删除前显示影响范围；本地日志和回执均不得含秘密。

### OPS-005 交接

任何新会话先读双文件和进度文件；实现者每次只推进一个可验收切片，更新阶段和证据。格式不支持、授权不明或越界功能写入 `BLOCKED.md`，不通过临时猜测绕过。

### OPS-006 任意区域首次比较

保持奥维客户端运行且图源已授权 → 打开历史影像四期对比 → 选择图源 → 点击“新建框选区域” → 绘制矩形/多边形并命名 → 使用此范围 → 系统自动选择四期 → 等待每屏加载回执 → 平移/缩放检查同步。若候选不足四期，保留缺口，不复制帧。双湖预设用于重复回归该旅程。

### OPS-007 变化判读

先记录“哪里、哪两个日期、看见什么变化”，再选择可能原因。只有补充水质、养殖范围、岸线/土地利用或政策等独立来源后，才能把原因从假设升级；升级记录来源、日期和适用范围。

### OPS-008 二次开发首次接入

启动本地网关 → 校验应用清单 → 用 SDK 列出源 → 读取所选源的能力 → 只调用已声明的日期或瓦片能力。看到 `metadata-only` 时返回图源管理完成探测/运行时绑定，不复制上游配置自行请求。升级 API 主版本前先跑契约测试；失败保留 source ID 和错误码以便恢复。

### OPS-009 一键四期与例外接管（已批准；尚未实现）

选择 confirmed 图源和 AOI → 点击“一键生成四期” → 驾驶舱自动推进。出现人工门时只完成页面明确要求的授权/凭证/内网/AOI/低置信裁决 → 点击继续。失败时查看步骤错误并只重试该步；刷新或重启后从原 run 恢复，不重新导入或重新框选。

## 数据与接口契约

### DATA-001 MapSourceDefinition

必含：`schemaVersion`、内部 UUID、`legacyId`、名称、来源类型、协议、投影/瓦片矩阵、级别范围、瓦片尺寸/格式、主机模板、路径模板、子域名、叠加关系、时间维度、非秘密参数、凭证引用、来源/授权声明、兼容扩展、创建/修改/最后验证时间和状态。原始字节不得直接成为渲染器输入。

### DATA-002 ImportReceipt

包含批次 ID、输入类型、输入 SHA-256、解析器及版本、图层结果列表、用户确认时间、探测摘要、渲染证据引用、错误类别和撤销状态；不含 token、Cookie、完整 Authorization、私钥或聊天凭据。

### DATA-003 ProbeResult

包含主机、请求协议、开始/结束时间、结果类别、HTTP 状态类别、内容类型、图片尺寸、限流/缓存提示和脱敏错误。响应正文不默认持久化。

### IF-001 导入检查接口

接收二维码载荷或 `.ovmap` 字节，执行纯本地解析并返回预览；不得联网。请求有尺寸限制和超时，错误使用稳定代码。

### IF-002 确认与探测接口

接收预览令牌、所选图层、用户授权确认和必要修正；重新校验后执行最小探测。预览令牌短时、一次性，不携带秘密到 URL。

### IF-003 瓦片代理接口

按已保存 source UUID 和 z/x/y 请求；后端从密钥存储解析凭证，执行域名/端口策略、限流、缓存和响应校验。客户端不能传任意上游 URL 形成开放代理。

### IF-004 开放导入导出接口

`.oms.json` 为文档化、版本化 JSON；开放二维码承载该 schema 的受限子集或指向用户控制的配置地址。任何私有奥维兼容扩展必须命名空间隔离。

### DATA-004 TemporalDateEntry

包含稳定 ID、请求日期、真实拍摄日期或 null、日期精度、可用状态和来源。年度目录只是筛选视图，不能覆盖源站真实日期。

### DATA-005 AreaOfInterest

包含 ID、版本、名称、EPSG:4326 Polygon/MultiPolygon、approximate/confirmed、来源、确认时间和几何哈希。截图像素不得直接冒充经纬度。

### DATA-006 ComparisonFrameReceipt

包含源、AOI 版本、请求/拍摄日期、ViewState、面板尺寸、loaded/partial/missing/failed、成功/失败瓦片数和时间；不保存真实瓦片或秘密。

### IF-005 时序源接口

提供 `probe()`、`listDates(aoi, from, to)` 和只对已知 dateId 生效的瓦片路由。前端不得传入任意上游 URL、主机或凭证。

### IF-006 AOI 与比较接口

`POST /api/aois` 只接收名称和 GeoJSON geometry，由网关生成 ID、版本、provenance 和确认时间；AOI 更新执行 GeoJSON 验证并追加版本。比较创建冻结源、AOI 版本、日期和 ViewState；帧事件只追加事实，不把前端“声称加载”直接当成源可用。

### DATA-007 DeveloperSourceDescriptor

包含 `apiVersion=v1`、source ID、显示名、provider kind、协议、投影、生命周期、`metadata-only/ready` 访问状态、能力集合、日期精度、版权/许可和本地 links。严格禁止 hosts、pathTemplate、queryParameters、credentialRef、sourceProvenance、compatibilityExtension 和原始输入。

### DATA-008 DeveloperAppManifest

包含 schemaVersion、应用 ID、名称、`apiVersion=v1`、所需能力和只读权限。V1 权限仅为读取源元数据、日期目录和瓦片；字段严格校验且不允许通配或任意网络权限。

### IF-007 开发者 V1 API

`GET /api/v1/developer/sources` 和单源路由返回 DATA-007；日期与瓦片子路由仅在 registry 中存在对应运行时适配器且能力允许时执行。所有链接都是本地相对路径；错误使用稳定 `source-not-found`、`capability-not-available`、`invalid-coordinate` 等代码。

### DATA-009 自动任务与步骤（已批准；V1 准备度子集 main）

`AutomationRun` 包含流程类型、source ID、AOI 版本、状态、当前步骤、进度、人工门、结果和 trace ID；`AutomationStep` 包含步骤类型、状态、尝试次数、幂等键、起止时间、输入/输出引用、错误码、是否外联和恢复建议。普通字段不得含秘密。

当前 V1 准备度子集的 run 实际字段为 schema/process/id/input fingerprint/source ID/名称/状态/当前步骤/下一动作/人工门/时间/四个 steps；step 实际字段为 kind/status/attempt/起止时间/externalRequest/errorCode/message/nextAction。AOI、进度、trace、输入输出引用和结果属于一键四期扩展，尚未进入 schema。

### DATA-010 帧质量（已批准；尚未实现）

`FrameQuality` 包含 date ID、日期精度、成功/失败瓦片数、AOI 覆盖率、内容指纹、空白/重复判断和可选云量。源未提供或无法测量的字段为 null，不能推断成 0。

### IF-008 异步任务 API（已批准；process/create/list/detail 子集 main）

任务接口借鉴 OGC API - Processes 的 process/execution/job/status/results/cancel 资源模型，提供创建四期任务、读取任务和事件、取得结果、恢复与取消；客户端不得传任意上游 URL、host、token 或跳过步骤。

当前实现仅提供 `GET /api/v1/processes`、`POST /api/v1/processes/source-readiness/execution`、`GET /api/v1/jobs` 和 `GET /api/v1/jobs/{jobId}`。创建请求严格只接受 source ID；events/results/resume/cancel 未实现。

## 非功能与产品质量

### NFR-001 安全

防 SSRF、DNS 重绑定、路径穿越、解压炸弹、超大二维码、任意协议、开放代理、日志泄密和前端直出 token。默认阻止 localhost、链路本地、私网、云元数据地址和非 HTTP(S)，合法企业内网需逐主机显式授权。

### NFR-002 隐私

V0 默认本地运行，不把导入文件、二维码、浏览区域或凭证上传到项目运营方。外部图源会看到必要的瓦片坐标、客户端出口 IP 和请求元数据，预览中应告知用户。

### NFR-003 性能与容量

先测基线再定硬数字。解析小型配置应提供即时进度；多图层逐项流式反馈；瓦片代理有并发、缓存和磁盘上限；不得因预览触发批量下载。

### NFR-004 可靠性与恢复

保存操作原子化；崩溃后不出现半活动图层；失败项可重试；回执可追溯；撤销只影响本批次。数据库备份/迁移在实现计划中定义并验证。

### NFR-005 兼容

支持现代桌面和移动浏览器；无摄像头或拒权时图片上传为等价路径。协议和 `.ovmap` 版本用适配器扩展，不在核心中散落条件判断。

### NFR-006 可访问性

关键流程支持键盘；状态不只依赖颜色；错误具有文本和下一步；扫码不是唯一入口；移动触控目标和桌面布局均可完成同一业务结果。

### NFR-007 可维护与开源

核心 schema、开放格式、错误码和兼容矩阵文档化；采用依赖前核许可证、维护和退出方案；缺许可证仓库只能作为行为证据，不能复制代码。

### NFR-008 可观测

健康、解析、探测、渲染和持久化分别可观察；日志结构化且脱敏；开发模式可关联回执 ID，生产默认不记录完整上游 URL 查询参数。

### NFR-009 多屏一致性

四屏在稳定状态下中心、缩放、旋转和投影必须数值一致；面板独立加载和失败。同步事件带来源标识并在下一动画帧合并，避免循环和卡顿。

### NFR-010 时序来源诚实

每一帧可追到源、日期精度、AOI 版本和加载事实；不能确定的拍摄日期显示未知。合成源、官方桥接和标准开放源分别标识，不能混报。

### NFR-011 资源边界

本地桥接和应用只绑定回环接口；默认缓存不超过 256 MiB；可用空间低于 8 GiB 停止新增依赖、构建、测试、截图和新增缓存；不批量抓取任意区域全域 20 年瓦片。

### NFR-012 SDK 兼容与最小暴露

V1 公共类型和错误语义通过契约测试冻结；新增可选能力不得破坏旧调用方，破坏性变更必须升主版本。开发者响应执行字段白名单序列化和秘密负面测试，不能直接透传内部 `MapSourceDefinition`。

### NFR-013 业务可观测性（已批准；准备度子集 main）

一次四期任务可用 run/step/receipt/trace ID 从触发追到结果。业务状态以持久结构化事件为真值；metrics 覆盖成功/部分/失败、步骤耗时、人工等待、重试、请求放大和真实可比期数。后续可接 OpenTelemetry traces/metrics，但日志文本不得成为任务状态源。

## 指标与观测

| 类型 | 指标 | 定义 | 当前状态 | 行动阈值 |
|---|---|---|---|---|
| 结果 | 首次真实出图成功率 | 合法、当前有效且格式已覆盖的首次导入中，最终到达 `rendered+saved` 的批次数 / 进入确认的批次数；排除用户取消和确认无权使用 | 先测基线 | 任一失败必须有可分类原因；不设拍脑袋百分比 |
| 过程 | 解析覆盖率 | 已解析图层数 / 合法样本中的图层总数，按 `.ovmap` 版本和二维码类型分群 | 先建样本账本 | 未知版本单独列，不并入成功 |
| 过程 | 可恢复失败率 | 有明确修正动作且重试成功的失败数 / 可恢复失败总数 | 先测基线 | 无下一步的常见错误进入阻塞修复 |
| 护栏 | 假成功数 | 未真实渲染却显示“可用/成功”的事件数 | 0 为硬门 | 任何 1 次阻止发布 |
| 护栏 | 秘密泄漏数 | 日志、回执、导出或 UI 非授权位置出现完整秘密的事件数 | 0 为硬门 | 任何 1 次阻止发布并轮换测试凭证 |
| 系统 | 源站请求放大 | 网关发出的上游请求数 / 用户可见必要瓦片数 | 先测基线 | 预览阶段必须为 0；探测阶段遵守 BR-006 |
| 结果 | 通用区域真实可比期数 | 每个确认 AOI 中完成真实非空加载且内容不重复的不同日期数 | 先由图源枚举 | 目标 4；不足时显示真实期数，不复制帧 |
| 质量 | 多屏对齐偏差 | 同一比较四屏 ViewState 数值差和面板像素尺寸差 | 自动测试冻结 | 稳定状态必须为 0；源影像正射误差单独提示 |
| 护栏 | 影像因果越级数 | 无外部证据却被标为确定污染/过度开发结论的观察数 | 0 为硬门 | 任一 1 次阻止 accepted |
| 结果 | 二次开发可消费源数 | 至少具备 `metadata` 且通过 V1 描述校验的唯一 source ID 数；按 `metadata-only/ready` 分群 | 先测基线 | 不把 metadata-only 计为可出图 |
| 护栏 | 开发者秘密暴露数 | 开发者 API/SDK/示例中出现上游模板、凭证引用、输入哈希或原始载荷的事件数 | 0 为硬门 | 任一 1 次阻止发布 |
| 过程 | 自动推进率 | 无需人工操作而完成的可自动步骤数 / 所有可自动步骤数；授权/秘密/内网/冲突人工门排除 | 先测基线 | 常见失败需有分类和恢复动作 |
| 质量 | 任务续跑正确率 | 重启或可重试失败后未重复外联/写入且正确恢复的任务数 / 续跑任务数 | 先测基线 | 任一重复秘密请求或业务写入阻止发布 |
| 结果 | 一键四期完成率 | confirmed source 与 confirmed AOI 创建的任务中达到真实 comparison receipt 的 run 数 / 有资格 run 数，按 blocked/partial/failed 分群 | 先测基线 | 合成源不并入真实分子 |

业务事件：`import_received`、`import_parsed`、`import_confirmed`、`source_probed`、`source_rendered`、`source_saved`、`import_failed`、`batch_undone`。事件只记录类型、回执 ID、解析器版本和错误类别，不记录原始载荷或秘密。

## 验收与晋级

### AC-001 真实二维码主旅程

目标用户从摄像头或二维码图片导入一张有权使用的真实奥维图源；预览字段正确，确认后真实瓦片显示，状态为已渲染和已保存。静态截图或 mock 图源不能单独通过。

### AC-002 `.ovmap` 多图层

用公开“腾讯地图5种.ovmap”或等价合法 fixture 验证：识别 `OviO`、有界解压并列出 5 个独立配置；解析阶段上游请求为 0。外部腾讯 URL 是否仍可用不作为该 fixture 的授权或渲染验收。

### AC-003 反例解析

截断文件、伪扩展名、错误魔数、未知版本、超高解压比例和异常字符串长度分别返回稳定错误；不联网、不崩溃、不产生活动图层。

### AC-004 秘密与安全

含测试 token 的二维码/文件在 UI、日志、回执、数据库普通字段和开放导出中均不出现完整值；恶意内网/云元数据 URL 被后端阻止。使用专用测试秘密，不使用真实长期凭证。

### AC-005 跨域真实出图

浏览器不直接暴露秘密，通过本地网关请求一个用户有权使用但浏览器直接访问受 CORS 限制的测试源，地图成功显示；网关不是任意 URL 开放代理。

### AC-006 持久化与回访

导入成功后刷新 Web UI、重启本地网关，图层定义、状态和回执仍存在；凭证引用可用但不回显；删除后对应定义和本地凭证不可再用。

### AC-007 桌面与移动等价

桌面浏览器使用拖入/图片完成主旅程，移动浏览器使用摄像头或图片完成同一结果；无摄像头环境仍可上传图片。

### AC-008 `.sdb` 缺失诊断

代表性本地航拍 `.ovmap` 在没有配套 `.sdb` 时被标记为 `needs-data`，不显示导入成功可浏览；用户可以保存非活动配置或取消。

### AC-009 部分成功和撤销

一个批次包含成功、需要凭证和不支持三类图层时，摘要逐项准确；撤销本批次只移除本次新建项，不影响此前数据。

### AC-010 开放导出

已保存配置导出为 `.oms.json` 和开放二维码后可重新导入恢复所有非秘密字段；接收者明确看到凭证需要自行配置。

### AC-011 奥维历史源桥接

官方奥维 10.6.0 已导入用户二维码并识别为历史影像源；在不暴露公网、不输出秘密的条件下，本机桥接对至少两个不同历史请求日期返回可解码、非空且哈希不同的真实瓦片。若只验证到导入或 HTTP 200，不通过。

### AC-012 任意区域 20 年四期比较

用户在真实地图创建一个非预设 AOI 后，工作台显示最近 20 个完整自然年的真实可用/缺失/未知状态；系统自动选择最多四个唯一日期进入四屏，同 AOI、同 ViewState，每屏显示日期精度和真实加载事实。至少两个、目标四个帧来自 AC-011 的真实源并具有不同归一化内容；合成图不能替代。

### AC-013 双湖回归比较

宝应湖和高邮湖两个独立预设都能进入同一通用旅程并保持各自几何、视角和比较状态；不得用湖泊 ID 分支决定 zoom、日期或协议，也不得把一个湖的结果复制为另一个湖的验收。

### AC-014 多屏对齐和错误隔离

在四屏任一面板平移、缩放、旋转后，稳定状态的中心、缩放、旋转和投影数值一致；AOI 版本一致。模拟一屏 404/429/超时，该屏显示失败，其他已加载面板保持可用，全局不得写成全部成功。

### AC-015 时间轴播放

时间轴按日期目录顺序播放可用帧，支持暂停、继续、前后帧和速度；缺失/失败保留可见并可跳到下一可用帧。只有真实非空瓦片加载后才产生 loaded 帧回执。

### AC-016 变化观察证据门

用户可以记录围网、岸线、水体颜色、裸地或建设等可见变化；污染、过度渔猎/养殖和过度开发在无独立来源时显示“假设，影像不能单独证明”。反例测试必须阻止无来源的确定因果标签。

### AC-017 二次开发脱敏与能力诚实

确认一个含测试上游 host/path/query、兼容扩展和凭证引用的代表性图源后，开发者列表与单源接口只能返回 DATA-007 白名单字段；秘密/内部字段序列化扫描为 0。该源未绑定运行时适配器时只具备 `metadata`，SDK 对日期或瓦片调用在 fetch 前返回 `capability-not-available`。

### AC-018 SDK 代表性消费旅程

示例应用以 V1 清单和 SDK 列出合成运行时源，读取日期目录并构造仅指向 `/api/v1/developer/...` 的本地瓦片 URL；源不存在、未知权限、未知 API 版本、查询参数和非法坐标反例稳定拒绝。该结果只证明 SDK/本地适配器契约，不替代真实奥维 AC-001/011。

### AC-019 任务状态真实可见（已批准；静态准备度部分 main）

从真实 QR/`.ovmap` 确认开始，驾驶舱逐步显示每个后台事实。故意制造凭证缺失、403、429、单帧空白和网关重启，状态、错误、重试和下一动作准确，其他成功步骤不回退。

阶段 1 验收只覆盖 confirmed、公网/私网/元数据静态策略、凭证元数据和 runtime binding 四种事实，以及四步零外联显示；403/429/空白帧属于真实探测阶段，当前未通过。

### AC-020 一键四期（已批准；尚未实现）

用户只完成授权、选择 source、框选 AOI 和必要人工门；系统自动得到最多四个真实、唯一、可解码且非空帧并生成比较回执。候选不足时显示真实数量和原因，不复制帧。

### AC-021 断点续跑与幂等（已批准；输入去重/重启恢复部分 main）

任务中重启网关或重复点击，恢复后不重复外部探测、不重复保存图源、不丢失已成功帧；只重试未完成或明确可重试步骤。

阶段 1 只证明同一源配置＋运行时状态指纹重复触发返回同一 run，原子 JSON 重开可恢复；由于当前任务零外联且一次求值，不构成步骤级断点续跑验收。

### AC-022 可解释选择与证据边界（已批准；尚未实现）

四期选择理由、日期精度、质量字段和未知项可见；算法变化检测只能生成候选。没有独立证据时，UI/API 均不能输出确定污染、过度养殖或过度开发结论。

### 晋级门

- `spec-approved`：用户复核本文与设计规格并明确批准。
- `local-candidate`：实现存在但任一冻结检查或真实旅程未通过。
- `local-verified`：按切片独立晋级。SDK 薄切片需 AC-017/018 的单元、路由、类型和构建通过；导入主线仍需 AC-001，历史主线仍需 AC-011 且 AC-012/013 至少达到真实源 local 旅程，三者不得互相替代。
- `main`：确切 commit 进入唯一 GitHub 主仓 `https://github.com/LegalInvest/open-map-bridge` 且真实 CI 通过；每个新提交必须独立取证，不继承旧 run 的成功。
- `deployed`：同一制品进入指定环境，配置、健康、日志、数据和回滚核验；当前没有部署授权。
- `accepted`：目标用户从实际入口独立完成二维码或 `.ovmap` 主旅程并签收；容器健康、HTTP 200 或开发者演示不能替代。

## 实施切片与变更记录

### 建议顺序

1. SLICE-V0-IMPORT-001：开放图源 schema、二维码图片/摄像头、`ovobj` 与 `.ovmap record37-zlib` 零外联解析、安全预览和确认保存。
2. SLICE-V0-SDK-001：脱敏源目录、能力协商、V1 本地 API、TypeScript SDK、应用清单和最小消费示例。
3. SLICE-V0-IMPORT-002：URL/SSRF 策略、凭证保险库、最小探测、瓦片代理和真实渲染；同一 source ID 绑定运行时能力。
4. SLICE-V0-IMPORT-003：部分成功、撤销、重启恢复、开放导出和真实用户签收。
5. SLICE-TEMPORAL-003：以已保存真实图源贯通本机 OviBridge/标准适配器的日期瓦片闸门，并通过 SDK 消费。
6. SLICE-TEMPORAL-005：真实导入与比较旅程稳定后增加 MP4/GIF 导出。
7. SLICE-AUTOMATION-RUN-001A（main，CI 已验证）：已建立 `source-readiness` 四步零外联任务、输入/运行时指纹去重、原子账本、process/job API 和驾驶舱；`16f445c` / CI `33155671827` 全绿，但不证明真实探测。
8. SLICE-AUTOMATION-RUN-001B（approved / next）：接入凭证保险库、DNS/IP 安全门、最小探测、真实运行时绑定和可恢复步骤推进。
9. SLICE-AUTOMATION-FOURFRAME-001（approved / backlog）：真实 source ID 绑定后实现一键四期、断点续跑、质量卡和来源链。

### 止损

- 同一验收连续失败三次，停止该路径并在 `BLOCKED.md` 记录证据和替代方案。
- 解析结果与奥维实际显示不一致时，不扩大样本批量；先固定一个最小 fixture 找到版本差异。
- 任何秘密泄漏、无确认外联、开放代理或大范围抓取行为立即阻止发布。

### 变更记录

- 2026-08-27：用户批准 Web UI＋本地网关 V0，要求二维码和 `.ovmap` 通用，首要目标为真实跑起来；建立当前完整 PRD。
- 2026-08-27：用户明确批准书面规格，晋级 `spec-approved`，开始编写实施计划；产品代码仍未开工。
- 2026-08-27：用户纠正“一个历史影像二维码可选择多个年份”，官方奥维 10.6.0 实际导入后识别为 GEE 协议历史影像并出现时间轴；产品从单图源导入扩展到时序源。
- 2026-08-27：用户批准方案 A 并指定宝应湖、高邮湖 2006–2025 为首批验收；增加 JRN-007 至 JRN-010、UI-005/006、BR-011 至 BR-015、FR-009 至 FR-013 和 AC-011 至 AC-016，进入 implementing。
- 2026-08-27：用户进一步收敛为“一次四张、展现 20 年”，并明确指出真正缺口是大规模跑通地理影像系统与奥维兼容；随后回复“按 A 执行”。当前切片改为任意框选区域、最近 20 个完整年、自动四期和真实奥维桥接；双湖降为样例与回归夹具。
- 2026-08-28：用户再次纠偏，明确项目核心首先是奥维图源导入，二维码扫描与 `.ovmap` 文件导入都必须实现。当前切片切回 `SLICE-V0-IMPORT-001`；已完成的通用四期消费端保留为下游回归，不能再被当作导入完成。
- 2026-08-28：用户明确“最终是要求能够基于奥维地图图源进行二次开发”。插件/行业应用开发者升级为 V0 直接角色；新增 JRN-011、UI-007、BR-017/018、FR-015/016、DATA-007/008、IF-007、NFR-012、OPS-008 和 AC-017/018。当前薄切片先冻结脱敏能力与 SDK 契约，真实奥维瓦片仍由 IMPORT-002 与 TEMPORAL-003 独立验收。
- 2026-08-28：用户提出“数字化之后，就是可视化和自动化”，要求提高两者程度。新增可观察任务、一键四期、人工门、幂等续跑、可解释选择和业务可观测提案；当前仍不改变 IMPORT-002 的真实源优先级，新增规格在用户审阅前为 proposed。
- 2026-08-28：用户回复“好的，根据讨论，继续优化架构和代码，记得更新全部文档”，批准上述方向继续实施。`source-readiness` 静态准备度任务和驾驶舱进入 local-candidate；一键四期、真实探测和完整续跑仍未完成。
- 2026-08-28：准备度切片提交 `16f445c` 进入 GitHub main；CI run `33155671827` 通过 108 Vitest＋2 Node、8 workspace typecheck、build 和 4 Chrome E2E，阶段晋级 main。一键四期、真实探测和完整续跑仍未完成。
- 2026-08-28：用户批准逐一修复全量审计问题并同步问题账本/进度 Markdown。建立 `docs/问题账本.md`（38 组问题），启动 `FIX-BATCH-001`：同 imported UUID 绑定、configured 不可消费、空 probe 不假绿和 source UUID/mapType/port 三元配置。当前只为 `local-candidate`；本机 5.8 GiB 容量门下未测试/构建，待 PR CI。
- 2026-08-28：`FIX-BATCH-001` 经 PR #1 合并为 `5a7e9ad`；CI `33159198541` 对源提交 `bc63661` 通过 113 Vitest＋2 Node、8 workspace typecheck、build 和 4 Chrome E2E。001/003/005/023 达到 main，002 仅假绿子项达到 main；真实探测/ready 仍开放。
- 2026-08-28：启动 `FIX-BATCH-002`，为 Ovi runtime 增加流式 5 MiB 上限、非 2xx 正文隔离、PNG/JPEG MIME/magic/完整解码/尺寸门和负例。当前仅 `local-candidate`，本机约 5.7 GiB 未运行测试/构建，待 PR CI。
- 2026-08-28：`FIX-BATCH-002` 经 PR #3 合并为 main `de36012`；首次 CI `33160137300`、第二次 `33160229102` 的失败已保留，第三次 `33160315934` 通过 122 Vitest＋2 Node、8 workspace typecheck、build 和 4 Chrome E2E。021 达到 main，002 仅解码前置达到 main；真实探测/ready 仍开放。
- 2026-08-28：启动 `FIX-BATCH-003`，为本地 gateway 增加精确回环 Host/Origin、DNS-rebinding/cross-site、Bearer、CSRF、服务端应用权限、限流和安全响应头候选；Vite 代理注入临时 UI token，SDK 使用独立 app token。当前仅 `local-candidate`，本机约 5.5 GiB 未测试/构建，待 PR CI。
- 2026-08-28：`FIX-BATCH-003` 经 PR #5 合并为 main `1d0ebc4`；CI `33161851375` 首轮通过 128 Vitest＋2 Node、8 workspace typecheck、build 和 4 Chrome E2E。020 达到 main；上游 DNS/IP、vault、真实探测/ready 仍开放。
- 2026-08-28：启动 `FIX-BATCH-004`，删除 Ovi 虚构年度日期目录并只接受已验证注入日期；无目录明确失败，未知日期 ID 零请求。当前为 `local-candidate`，本机约 5.4 GiB 未测试/构建，待 PR CI；真实日期目录提供者和真实影像仍开放。
- 2026-08-28：`FIX-BATCH-004` 经 PR #7 合并为 main `5b6f06e`；CI `33163589956` 首轮通过 36 个 Vitest 文件/128 tests＋2 Node、8 workspace typecheck、build 和 4 Chrome E2E。004 的不伪造/无目录失败规则达到 main；真实目录提供者、probe/ready 和真实影像仍开放。
- 2026-08-28：启动 `FIX-BATCH-005`，把时序实际日期、顺序窗口、AOI/date ID 和瓦片坐标下沉为共享 schema，接入旧 API、V1、SDK 与两个适配器。当前为 `local-candidate`，本机约 5.3 GiB 未测试/构建，待 PR CI；零真实外联。
- 2026-08-28：PR #9 首次 CI `33164557423` 在 161 个 Vitest 中通过 159 个；两个 161 字符 dateId 路径被 Fastify 在路由前以 414 拒绝，原测试误期望 400。保留该安全门并把路由断言改为 414；package/SDK 继续验证 160 字符业务契约，等待全门重跑。
- 2026-08-28：PR #9 第二轮 CI `33164783702` 通过 37 个 Vitest 文件/162 tests＋2 Node、8 workspace typecheck、生产构建、4 Chrome E2E 和交底新鲜度，随后合并为 main `f84ae20`。OMB-AUD-022 达到 main；真实奥维源、部署和用户验收不随之晋级。
- 2026-08-28：FIX-BATCH-005 证据 PR #10 经 CI `33165036211` 合并，main 为 `f8733da`；启动 FIX-BATCH-006，统一 `.ovmap` 1 MiB 文件、base64 和 HTTP 信封边界，当前仅 `local-candidate`，待 PR CI。
- 2026-08-28：PR #11 CI `33165515010` 首轮通过 37 个 Vitest 文件/167 tests＋2 Node、8 workspace typecheck、生产构建、4 Chrome E2E 和交底新鲜度，随后合并为 main `cfab0c2`。OMB-AUD-010 达到 main；QR 图片资源门、预览 LRU、真实图源与用户验收不随之晋级。
- 2026-08-28：FIX-BATCH-006 证据 PR #12 经 CI `33165750762` 合并，main 为 `98a9828`；启动 FIX-BATCH-007，增加预览 LRU 和二维码图片解码前资源门，当前仅 `local-candidate`，待 PR CI。
- 2026-08-28：PR #13 首次 CI `33166210759` 单测通过后在 Web 类型检查因测试 `Uint8Array<ArrayBufferLike>` 不满足 BlobPart 失败；构建/Chrome 未运行。测试 helper 改为明确 ArrayBuffer，失败保留并待重跑。
- 2026-08-28：PR #13 第二轮 CI `33166313733` 通过 39 个 Vitest 文件/175 tests＋2 Node、8 workspace typecheck、生产构建、4 Chrome E2E 和交底新鲜度，随后合并为 main `873705b`。OMB-AUD-018 达到 main；相机帧、真实图源、部署和用户验收不随之晋级。
- 2026-08-31：用户授权 Codex 自主推进，直到本机、GitHub、服务器均为最新可验收状态。主线仍是 `QR/.ovmap → 安全预览 → 确认 → 探测/ready → 真实渲染 → 四期消费 → SDK`，不以解析成功、CI 绿或服务器 HTTP 200 替代真实源验收。当前启动 `FIX-BATCH-008 / OMB-AUD-039`，先收敛已观察 QR 方言兼容且不保存未知值。
- 2026-08-31：FIX-BATCH-008 已进入 main；启动 FIX-BATCH-009，将服务器交付前置条件冻结为独立 gateway/Web 制品、确定性 manifest、鉴权健康、原子持久化、结构化生命周期、SIGTERM 正常退出、loopback 反向代理和可恢复回滚。公网开放不在该契约内，不以服务健康替代真实源验收。
- 2026-08-31：FIX-BATCH-009 已进入 main。首个服务器目标经只读比较选定腾讯机；采用 versioned release、项目专用 Node 24 runtime、systemd gateway、loopback nginx 和 SSH tunnel，保持公网开放为禁用状态。部署验收仍必须与真实奥维源业务验收分开。
