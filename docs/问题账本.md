# OpenMapBridge 全量问题账本

## 文档控制

- 账本 ID：`OMB-ISSUES-001`
- 建立时间：2026-08-28 17:12（Asia/Shanghai）
- 最后更新：2026-08-31 19:08（Asia/Shanghai）
- 当前证据分支：`codex/fix-batch-012-credential-vault`
- GitHub 证据基线：部署证据 commit `a92ff8c`、main CI `33382682547` 全绿；runtime source/current `94e42b1` 的 artifact/state/health/双回环门通过
- 已完成批次：`FIX-BATCH-001/002/003/004/005/006/007/008/009/010/011`；当前批次：`FIX-BATCH-012 encrypted credential vault + OMB-AUD-040 local-candidate`
- 验证限制：根卷约 7.15 GiB，低于 8 GiB；本机构建、测试、浏览器、下载、影像和新制品部署已暂停。vault 核心保留先前 local-verified 证据，最终分支等待 GitHub CI

## 状态与阶段口径

- `open`：已发现，尚未修复。
- `in-progress`：正在修改，尚未形成完整候选。
- `partial`：只修复了问题的一部分，剩余验收项仍开放。
- `local-candidate`：源码和回归测试已写入分支，但本机门禁未验证。
- `local-verified`：本机相关测试/检查通过；不等于 GitHub main。
- `main`：修复和对应门已进入 GitHub main；不等于部署或用户验收。
- `blocked`：缺少容量、合法真实源、人工授权或外部能力，且不能安全假设。
- `closed`：该问题定义的验收项全部达到；后续回归仍可重开。

阶段必须按 `discovered → local-candidate → local-verified → main → deployed → accepted` 分开记录。CI 通过最多证明该提交的自动门通过，不能证明真实奥维源、真实历史日期、真实影像或用户业务验收。

## P0：阻断真实图源主链

| ID | 问题 | 当前状态 | 当前证据/修复 | 完成验收 |
|---|---|---|---|---|
| OMB-AUD-001 | confirmed 导入源使用 UUID，但 runtime/历史页/开发者目录使用另一个硬编码 ID，主链断裂 | `main` | `5a7e9ad` 按 persisted UUID 绑定；相同 legacyId 隔离、重启恢复和目录合并测试由 CI `33159198541` 通过 | 导入、准备度、目录、日期、瓦片和回执始终引用同一 UUID；重启后仍成立 |
| OMB-AUD-002 | OviBridge 仅配置即长期标为 configured，且 `probe()` 未请求/解码真实瓦片却返回 `ok=true` | `partial / deployed` | 空 probe 失败、`de36012` 图片门和 FIX-BATCH-011 的显式已登记 probe→ready 编排已进入 runtime source `94e42b1` 并部署为腾讯 current；PR/main CI、本地 production smoke、artifact/state/health/双回环门全绿。ProbeResult 持久化和真实源验证仍缺 | 只有受控请求取得可解码、非空、符合内容类型的真实瓦片才变为 ready；失败保留证据 |
| OMB-AUD-003 | configured Ovi 被旧时序 API/历史 UI 当作可用，并显示“本机授权源” | `main` | 时序目录只返回 ready；configured dates/tiles 409；无 ready 源有明确空态；CI `33159198541` 通过 | configured 在所有消费入口均不可调用且显示明确阻塞；ready 后才出现 |
| OMB-AUD-004 | Ovi `listDates` 按年伪造 unknown 目录，忽略 AOI/真实年份并固定 6 月 30 日 | `main` | `5b6f06e` 删除年度生成器；只接受严格 schema＋唯一 ID 的注入日期；无目录明确失败，未登记 ID 与 `missing/failed` 在零请求下返回未找到；CI `33163589956` 通过。真实目录提供者仍是产品阻塞 | 从已授权接口取得真实可请求日期/时间标识；无法取得时明确“不支持/未知”，不伪造目录 |
| OMB-AUD-005 | Ovi runtime 和启动配置硬编码 mapType 200 | `main` | `OMB_OVI_PORT/OMB_OVI_MAP_TYPE/OMB_OVI_SOURCE_ID` 成组校验并只绑定显式 UUID；CI `33159198541` 通过 | 非 200 合法 mapType 可配置且只绑定显式 UUID；同 legacyId 不串绑；缺项/非法值启动失败 |
| OMB-AUD-006 | gateway 使用 `tsc --noEmit`，没有可交付的生产启动制品/部署契约 | `main` | PR #16 合并为 `b40ed0d`；PR CI `33369111954` 与 main CI `33369227321` 均验证独立制品、鉴权健康、持久化和 SIGTERM | 生成可复现服务制品、生产启动命令、健康/优雅停机和部署回滚说明 |
| OMB-AUD-039 | 已成功解码的公开 `ovobj` 二维码大量含 `hs/mf/ml/ms/mt/pn/pt`，旧适配器将其作为未知键整体拒绝 | `main` | PR #15 合并为 `382e0e6`；PR CI `33368112824` 与 main CI `33368228496` 全绿。29/29 已解码公开 QR 通过，5 张保持图像解码失败，七键值不返回/持久化 | 已观察方言不因这七键失败；值不进入预览/日志/持久层；重复和未观察键 fail closed；正反例及公开样本内存回放通过 |

## P1：正确性、安全性、恢复性与通用性

| ID | 问题 | 当前状态 | 当前证据/影响 | 完成验收 |
|---|---|---|---|---|
| OMB-AUD-007 | QR/OMS 归一化丢失 maxZoom、projection、protocol、tileSize、hosts、attribution、license 等事实 | `open` | 多字段被固定为 unknown/0/256 或单 host | 逐适配器保留“原始事实/推断/用户修正”并有正反 fixture |
| OMB-AUD-008 | 开放源模型缺 transport scheme；常量 query 被全部视为凭证并丢弃 | `open` | 无法准确表达 http/https 和合法常量参数 | 模型显式表达 scheme、非秘密常量和秘密引用，导出/请求一致且不泄密 |
| OMB-AUD-009 | `.ovmap` 无可靠家族/版本判别，投影/图片代码硬编码，maxZoom 静默截断，单坏记录拖垮整批 | `open` | 仅验证 `record37-zlib` 样本族 | 未知族 fail closed；逐图层部分成功；字段差异有 fixture；不静默改写事实 |
| OMB-AUD-010 | Fastify 默认 1 MiB body limit 与 1 MiB 文件 base64 约 1.4 MiB 冲突 | `main` | `cfab0c2` 共享 1 MiB 原文件、精确 base64 和 4 KiB JSON 信封常量；仅 `.ovmap` 路由使用计算后 body limit；前端读取前拒绝超限，后端区分文件/信封两种稳定 413。CI `33165515010` 全绿 | 文件/编码/HTTP 上限一致，有恰好 1 MiB、1 MiB＋1 字节和信封超限测试，错误稳定 |
| OMB-AUD-011 | 任意 AOI 创建器移除常规地图交互、首屏强制预设，且必须先有源/日期才能画 | `open` | 通用框选旅程被夹具和加载顺序限制 | 无源也可平移缩放和绘制任意区域；预设只是可选 fixture |
| OMB-AUD-012 | AOI 拓扑校验不完整，接受 Mercator 极区；编辑/显示只处理首个外环并会破坏 MultiPolygon/洞 | `open` | 复杂几何会失真或被覆盖 | Polygon/MultiPolygon/洞完整往返；Web Mercator 纬度门；拓扑错误可解释 |
| OMB-AUD-013 | 时间轴在 missing/unknown 时标签推进而画面不变；unknown Ovi 不会播放 | `open` | 标签与当前影像事实可分离 | 播放帧、标签、请求和逐屏状态原子对应；缺失跳过/失败不冒充切换 |
| OMB-AUD-014 | MapPane 任一瓦片成功即整屏 loaded，掩盖部分失败 | `open` | 不能区分完整、部分、失败 | 统计 expected/loaded/failed，独立显示 partial，并写入质量回执 |
| OMB-AUD-015 | ComparisonReceipt 只有 GET，UI 不持久化；ObservationPanel 保存按钮无业务动作 | `open` | 观察和比较不能回访/审计 | 创建/读取比较与观察 API、引用完整帧/AOI/source，刷新和重启恢复 |
| OMB-AUD-016 | state 中 receipt/frame 仅浅校验，缺少 source/AOI/date 引用完整性 | `open` | 可持久化悬空或互相矛盾事实 | 入口和加载时做 schema＋引用完整性验证，错误可恢复且不静默丢弃 |
| OMB-AUD-017 | 原子 JSON 只保证单进程序列化，跨进程可能覆盖；无顶层版本/备份/目录 fsync/分页 | `open` | 数据增长与崩溃恢复能力不足 | 单写者锁或事务库、schema migration、可恢复备份、分页和并发测试 |
| OMB-AUD-018 | 预览内存无数量上限；QR 图片缺文件/像素上限且 2x/3x 解码可能造成资源耗尽 | `main` | `873705b` 增加预览主动 TTL 清理、64 条/4 MiB 双限 LRU；QR 图片在 object URL/ZXing 前限制 8 MiB、16,777,216 像素并解析 PNG/JPEG/WebP 文件头；2×/3× 受缩放后像素预算约束。首次 CI `33166210759` 红灯保留，`33166313733` 全绿 | 预览 TTL＋数量/字节 LRU；图片在解码前限制字节/像素/倍率并测拒绝路径 |
| OMB-AUD-019 | confirm 在完整验证前 consume preview；存储/磁盘错误也被映射成“预览失效”400 | `open` | 用户可能无故失去预览且诊断错误 | 验证后消费或可幂等重试；输入、冲突、容量、存储错误分别编码 |
| OMB-AUD-020 | 本地 gateway 缺 Host/Origin/鉴权/CSRF/DNS rebinding/rate 门；SDK manifest 只是客户端约定 | `main` | `1d0ebc4`：精确回环 Host/Origin/Fetch-Site、Bearer、CSRF、固定窗口限流、服务端 app 权限和 Vite 来源保真；CI `33161851375` 通过 | 服务端回环/Host/Origin/token/权限/限流门和浏览器攻击反例通过 |
| OMB-AUD-021 | Ovi 响应无 content-length 时可能无界读取；非 2xx body 被转发；runtime 不验证图片可解码 | `main` | `de36012`：流式 5 MiB 硬上限、只接受 200、错误正文丢弃、PNG/JPEG MIME＋magic＋解码＋2048 维度门；CI `33160315934` 通过 | 流式硬上限、状态白名单、magic/decode/dimension 校验、错误正文不外泄 |
| OMB-AUD-022 | 时序日期、窗口、dateId、AOI ID 和瓦片坐标校验不一致 | `main` | `f84ae20` 将实际 ISO 日期、顺序窗口、160 字符无控制符 ID、规范十进制安全整数、z≤30、x/y<2^z 下沉到共享 schema；旧 API、V1、SDK、Ovi/合成适配器复用；HTTP 层保留更早的超长参数 414。CI `33164783702` 全绿 | V1/旧 API 共用严格 schema；ISO 实际日期、窗口、z/x/y 上界与长度全覆盖；超长路径参数可由 HTTP 层先以 414 fail closed |
| OMB-AUD-023 | readiness 曾按 legacy mapType 冲突寻找 runtime，而非同一 source UUID | `main` | `runtimeFor()` 改为 exact ID；相同 legacyId 反例由 CI `33159198541` 通过 | 两个相同 legacyId 的源不串绑；只有 exact UUID 能推进 |
| OMB-AUD-024 | TypeScript SDK 私有且 source-only，无 build/publish/export 交付物 | `open` | 外部项目无法可靠安装消费 | 版本化构建、exports/types、包测试、发布/本地 tarball 接入文档 |
| OMB-AUD-025 | 授权 QR Playwright trace 可能保留真实二维码画面/载荷 | `open` | 本地验收制品存在秘密持久化风险 | 授权门默认禁 trace/video/screenshot，失败证据脱敏且清理策略可验证 |
| OMB-AUD-026 | Playwright `reuseExistingServer:true` 可复用旧代码/旧状态，绕过临时状态隔离 | `open` | E2E 可能测试错进程 | CI 和权威本地门强制新进程、唯一状态路径、版本握手；复用仅显式开发模式 |
| OMB-AUD-027 | 缺可验证的生产 gateway build/start 路径 | `main` | `b40ed0d` 在 Linux CI 将制品复制到独立 release，以 `node server.mjs` 启动，结构化启动/停机日志齐全且 SIGTERM 退出码 0 | 安装后不依赖 TS 源码即可启动 gateway，并有冒烟/停机/日志门 |

## P2：工程治理、供应链、性能与文档真值

| ID | 问题 | 当前状态 | 当前证据/影响 | 完成验收 |
|---|---|---|---|---|
| OMB-AUD-028 | 公共 CI 不运行 compatibility 与 authorized gates | `open` | CI 绿不覆盖真实格式/授权旅程 | 无秘密 fixture 进入公共兼容门；授权门有安全的独立运行与结果口径 |
| OMB-AUD-029 | main 无保护；Dependabot 安全更新关闭；无 code scanning；Actions 未按 SHA 固定 | `open` | GitHub 治理和供应链门不完整 | 分支保护/规则集、依赖安全更新、扫描、最小权限和 SHA 固定完成并取证 |
| OMB-AUD-030 | 无 lint、覆盖率、SBOM、第三方 notices 和依赖安全审计 | `open` | 质量/许可证/供应链不可持续审计 | 建立分层门和制品，阈值不制造假绿，第三方许可证可追溯 |
| OMB-AUD-031 | 环境门在 install 之后，且未验证 npm 版本 | `main` | `b40ed0d` 的 PR/main CI 均在 `npm ci` 前检查 Node 24–26、npm 11 和至少 8 GiB；正反例覆盖 Node/npm/容量 | 安装前轻量门校验 Node/npm/磁盘，并在 CI 与文档一致 |
| OMB-AUD-032 | dev orchestration 可能遗留子进程；E2E 临时状态持续累积 | `open` | 长期运行消耗端口和磁盘 | 进程组优雅清理、临时目录生命周期和中断测试通过 |
| OMB-AUD-033 | Web bundle 约 1.129 MB minified / 316.76 KB gzip；Swipe 拖动时反复创建地图 | `open` | 首载和交互成本偏高 | 路由/地图懒加载、预算门；Swipe 复用 map，仅更新裁剪状态 |
| OMB-AUD-034 | fixture 下载在 hash 校验前无界读取 | `open` | 测试/研究工具可被超大响应耗尽 | 流式大小上限、超时、hash 后原子落盘，负例覆盖 |
| OMB-AUD-035 | 技术交底指纹不覆盖 CI、README、SECURITY、runbook、验收、锁文件和关键配置 | `main` | `b40ed0d` 指纹集覆盖 CI、Node、README/SECURITY/CONTRIBUTING、deploy、锁文件、TS/Playwright/Vitest、进度/阻塞、验收/自动化/部署/runbook/问题账本；CI 新鲜度通过 | 指纹集覆盖所有架构/发布/安全/验收来源，排除只限生成物 |
| OMB-AUD-036 | 文档存在历史漂移/过度陈述：旧分支/无 remote/测试数、localhost、可解码 Ovi、schema/renderer、观察持久化等 | `in-progress` | 本账本和四份权威文档开始纠偏；仍需逐页校对 | README/goal/research/runbook/schema/交底/验收与当前代码逐项一致，链接门通过 |
| OMB-AUD-037 | synthetic 源始终注册并默认选中，虽有提示仍可能被误当生产能力 | `open` | 演示 fixture 与真实源同处默认运行时 | 显式 demo/acceptance 模式；生产默认不注册 synthetic；UI/回执不可混淆 |
| OMB-AUD-038 | 列表/API 缺分页、速率/并发控制与业务可观测，状态可无限增长 | `open` | 长期使用性能和运维不可控 | 游标分页、配额/背压、结构化指标与容量告警覆盖主要集合和任务 |
| OMB-AUD-040 | root 重型命令不自动运行 8 GiB 环境门；操作者可只打印 `df` 后继续构建/测试 | `local-candidate` | 19:08 低容量读数后一次组合命令仍启动 build/smoke；该结果不计最终验证。候选为 test/typecheck/build/dev/production smoke/compat/E2E/fixture acquisition 增加 npm `pre*` 环境门并冻结脚本映射 | 所有受支持重型根命令在低于 8 GiB 时先失败且不启动工作；CI 静态测试验证接线，低容量执行证据保留 |

## 已批准但尚未实现的产品能力

以下是公开 backlog，不应伪装成隐藏缺陷，也不能因不在上述 40 项中而消失：本地凭证保险库核心已形成 FIX-BATCH-012 local-verified 证据，但最终分支因 OMB-AUD-040 守卫更新为 local-candidate，且尚未 main/deployed；请求时 DNS/IP/连接固定/重绑定门；真实最小探测与 runtime ready 晋级；真实日期目录与一次四张；标准 XYZ/TMS/WMTS/WMS/STAC 适配和投影修正；撤销/开放导出/`.sdb`/更多 `.ovmap` 家族；一键四期、续跑、取消、质量与来源链；双湖真实证据；MP4/GIF 等可追溯输出。

## 修复批次与进度

| 批次 | 范围 | 分支/提交 | 阶段 | 验证 | 结果 |
|---|---|---|---|---|---|
| FIX-BATCH-001 | OMB-AUD-001/002/003/005/023：同 UUID 绑定、configured 真值、source/mapType/port 三元配置 | PR #1 / `5a7e9ad` | `main` | CI `33159198541`：113 Vitest＋2 Node、8 workspace typecheck、build、4 Chrome E2E 全绿 | 001/003/005/023 修复进入 main；002 只关闭假绿子项，真实 ready 仍开放；deployed/accepted 未达到 |
| FIX-BATCH-002 | OMB-AUD-021＋OMB-AUD-002 解码前置：Ovi runtime 响应真实性/资源门 | PR #3 / `de36012` | `main` | CI `33160315934`：122 Vitest＋2 Node、8 workspace typecheck、build、4 Chrome E2E 全绿 | 021 修复进入 main；002 只完成解码前置，真实 probe/ready 仍开放；deployed/accepted 未达到 |
| FIX-BATCH-003 | OMB-AUD-020：本地 gateway Host/Origin/Bearer/CSRF/服务端权限/限流 | PR #5 / `1d0ebc4` | `main` | CI `33161851375`：128 Vitest＋2 Node、8 workspace typecheck、build、4 Chrome E2E 全绿 | 020 修复进入 main；不发真实请求；官方 Web token 不入 browser bundle；deployed/accepted 未达到 |
| FIX-BATCH-004 | OMB-AUD-004：删除 Ovi 虚构年度日期目录，仅允许已验证目录输入 | PR #7 / `5b6f06e` | `main` | CI `33163589956`：36 个 Vitest 文件/128 tests＋2 Node、8 workspace typecheck、build、4 Chrome E2E 全绿 | 004 的不伪造/无目录失败规则进入 main；真实目录提供者、probe/ready、deployed/accepted 未达到 |
| FIX-BATCH-005 | OMB-AUD-022：统一时序日期窗口、ID 和瓦片坐标真值 | PR #9 / `f84ae20` | `main` | CI `33164783702`：37 个 Vitest 文件/162 tests＋2 Node、8 workspace typecheck、build、4 Chrome E2E、交底新鲜度全绿；首次红灯 `33164557423` 保留 | 022 修复进入 main；拒绝伪日期、反向窗口、空白/超长 ID、非规范/越界瓦片；超长 URL 更早 414；零真实外联；deployed/accepted 未达到 |
| FIX-BATCH-006 | OMB-AUD-010：统一 `.ovmap` 原文件、base64 和 HTTP 信封上限 | PR #11 / `cfab0c2` | `main` | CI `33165515010`：37 个 Vitest 文件/167 tests＋2 Node、8 workspace typecheck、build、4 Chrome E2E 和交底新鲜度全绿 | 010 修复进入 main；恰好 1 MiB 到达格式解析，多 1 字节和超信封稳定 413，不扩大其他 API；deployed/accepted 未达到 |
| FIX-BATCH-007 | OMB-AUD-018：限制预览缓存与 QR 图片解码资源 | PR #13 / `873705b` | `main` | CI `33166313733`：39 个 Vitest 文件/175 tests＋2 Node、8 workspace typecheck、build、4 Chrome E2E、143 文件交底新鲜度全绿；首次红灯 `33166210759` 保留 | 018 修复进入 main；过期主动释放、LRU 双限、图片在 URL/ZXing/canvas 前 fail closed；零原图上传；相机帧不在本项 |
| FIX-BATCH-008 | OMB-AUD-039：兼容已观察 `ovobj` 扩展键且不泄露未知值 | PR #15 / `382e0e6` | `main` | PR CI `33368112824` 与 main CI `33368228496` 全绿；本地公开样本 29/29 已解码 QR 通过，5 张图像解码失败 | OMB-AUD-039 进入 main；格式兼容不等于图源授权、可达或渲染 |
| FIX-BATCH-009 | OMB-AUD-006/027/031/035：独立生产制品、生命周期、部署回滚、安装前环境门和交底覆盖 | PR #16 / `b40ed0d` | `main` | PR CI `33369111954` 与 main CI `33369227321` 全绿：安装前环境门、178 Vitest＋2 Node、8 typecheck、build、production smoke、4 Chrome E2E | 006/027/031/035 进入 main；不把技术部署冒充真实图源验收 |
| FIX-BATCH-010 | 腾讯服务器私有 loopback 部署、项目专用 Node runtime、健康/重启/隧道验收 | PR #17/#18 / main `62ab114` / 腾讯 current `62ab114` | `deployed`（真实源 accepted 未达） | CI `33370752935`；artifact 哈希、项目 Node 24.19、systemd hardening、双回环监听、UI/API/401、状态 600、生命周期日志、Web 四屏/播放和 `62ab114→33f7f06→62ab114` 通过 | nginx include 红灯已保留；回滚/前滚时 state hash 不变；真实 Ovi probe/ready/render/用户签收仍开放 |
| FIX-BATCH-011 | OMB-AUD-002：Ovi 最小真实 probe 与 runtime ready 编排 | PR #20 / runtime source `94e42b1` / 腾讯 current `94e42b1` | `deployed-code / partial` | PR CI `33379147631`、main CI `33379302430`、本地 production smoke、artifact/state/health/双回环门全绿；无 probe/403/损坏图片保持 configured，合法 PNG 通过后同 UUID 获得 ready 能力 | 未请求用户真实源；未持久化 ProbeResult；真实目录、vault、DNS/IP/重绑定仍开放 |
| FIX-BATCH-012 | Approved backlog / BR-004＋OMB-AUD-040：本地加密凭证 vault、同 source UUID 引用与重型命令自动容量门 | `codex/fix-batch-012-credential-vault` | `local-candidate`（vault 核心曾 local-verified） | 容量满足时 196 Vitest＋2 Node、8 workspace typecheck、production build/smoke、4 Chrome E2E；19:08 后仅新增 npm `pre*` 门与静态契约，因约 7.15 GiB 未本地重跑，待 GitHub CI | 尚未 main/deployed；低容量误跑结果不作最终验证；未存真实凭证、未发外联；请求时 DNS/IP/连接固定/重绑定和凭证注入仍开放 |

## 追加式更新记录

| 时间 | 事件 | 账本变化 | 证据阶段 |
|---|---|---|---|
| 2026-08-28 17:12 | 用户要求逐一修复并同步问题账本/进度 Markdown | 建立 38 项审计账本；启动 FIX-BATCH-001 | `discovered → local-candidate`，待 CI |
| 2026-08-28 17:26 | PR #1 合并 | FIX-BATCH-001 进入 `5a7e9ad`；更新 001/003/005/023 为 main，002 为 partial/main | CI `33159198541` 全绿；非真实源验收 |
| 2026-08-28 17:32 | 启动 FIX-BATCH-002 | 增加 Ovi 响应流式截断、非 2xx 隔离、PNG/JPEG 解码/尺寸候选与反例 | `local-candidate`；待 PR CI |
| 2026-08-28 17:38 | PR #3 首次 CI 红灯 | CI `33160137300` 中合法 PNG/JPEG 的 `Buffer` 对 `Uint8Array` 深比较失败；解码路径已执行，改为逐字节断言 | 失败保留；仍为 `local-candidate`，等待重跑 |
| 2026-08-28 17:39 | PR #3 第二次 CI 红灯 | CI `33160229102` 单测越过后，严格类型检查拒绝测试里的 `Uint8Array<ArrayBufferLike>` 作为 `BodyInit`；显式复制为 `ArrayBuffer` | 失败保留；仍为 `local-candidate`，等待重跑 |
| 2026-08-28 17:41 | PR #3 第三次 CI 全绿并合并 | FIX-BATCH-002 进入 main `de36012`；021 更新为 main，002 保持 partial/main | CI `33160315934` 通过 122 Vitest＋2 Node、8 workspace typecheck、build、4 Chrome E2E；非真实源验收 |
| 2026-08-28 17:58 | 启动 FIX-BATCH-003 | 增加入站 Host/Origin/DNS-rebinding、Bearer、CSRF、服务端 app 权限、限流和安全响应头候选；SDK 改用独立令牌取瓦片 | `local-candidate`；待 PR CI，不执行真实外联 |
| 2026-08-28 18:04 | PR #5 首轮 CI 全绿并合并 | FIX-BATCH-003 进入 main `1d0ebc4`；020 更新为 main | CI `33161851375` 通过 128 Vitest＋2 Node、8 workspace typecheck、build、4 Chrome E2E；非真实源验收 |
| 2026-08-28 18:11 | 启动 FIX-BATCH-004 | 删除 Ovi 年度日期生成器；无已验证目录明确失败，未知日期 ID 零请求返回未找到，只过滤返回注入的严格日期项 | `local-candidate`；待 PR CI；未执行真实外联，真实目录仍 blocked |
| 2026-08-28 18:28 | 本批交底静态门先红后绿 | 首次指纹更新因 git 索引仍列出已删除年度文件而 `ENOENT`；脚本改为忽略工作树中已删除项后，137 文件指纹 `64873c34d881` 更新并检查通过 | 失败已保留；仅静态门通过，不替代 PR CI，也不关闭 OMB-AUD-035 的覆盖范围问题 |
| 2026-08-28 18:32 | PR #7 首轮 CI 全绿并合并 | FIX-BATCH-004 进入 main `5b6f06e`；004 更新为 main | CI `33163589956` 通过 36 个 Vitest 文件/128 tests＋2 Node、8 workspace typecheck、build、4 Chrome E2E；零真实外联，非真实源验收 |
| 2026-08-28 18:40 | 启动 FIX-BATCH-005 | 建立共享日期窗口/ID/瓦片 schema，并接入旧时序 API、V1、SDK、Ovi 与合成适配器；补实际日期、规范路径整数和边界反例 | `local-candidate`；待 PR CI；未执行真实外联 |
| 2026-08-28 18:47 | PR #9 首次 CI 红灯 | CI `33164557423` 共 161 个 Vitest，159 通过；两个 161 字符 dateId 路径在业务路由前被 Fastify 默认参数长度门以 414 拒绝，测试原预期 400。共享 schema/SDK 仍覆盖 160 字符契约；不放宽服务器门，改为显式验证更早的 HTTP 414 | 失败证据保留；仍为 `local-candidate`，等待重跑；类型、构建和浏览器门尚未执行 |
| 2026-08-28 18:51 | PR #9 第二次 CI 全绿并合并 | FIX-BATCH-005 进入 main `f84ae20`；022 更新为 main | CI `33164783702` 通过 37 个 Vitest 文件/162 tests＋2 Node、8 workspace typecheck、生产构建、4 Chrome E2E 和 139 文件交底新鲜度；零真实外联，非真实源验收 |
| 2026-08-28 18:56 | 启动 FIX-BATCH-006 | 将 1 MiB 原文件、base64 长度和 JSON 信封上限固化为共享常量；只提高 `.ovmap` 路由门，增加前端预检、解码后超限和 HTTP 超限稳定错误及边界反例 | `local-candidate`；待 PR CI；未读取真实文件、未执行真实外联 |
| 2026-08-28 19:02 | PR #11 首轮 CI 全绿并合并 | FIX-BATCH-006 进入 main `cfab0c2`；010 更新为 main | CI `33165515010` 通过 37 个 Vitest 文件/167 tests＋2 Node、8 workspace typecheck、生产构建、4 Chrome E2E 和交底新鲜度；零真实文件/外联，非真实源验收 |
| 2026-08-28 19:09 | 启动 FIX-BATCH-007 | 预览缓存增加主动 TTL 与数量/估算字节 LRU；QR 图片增加字节、文件头尺寸、像素和缩放后像素门，限制在创建 URL、ZXing 与 canvas 前 | `local-candidate`；待 PR CI；未读取用户图片、未启动浏览器、未执行外联 |
| 2026-08-28 19:12 | PR #13 首次 CI 红灯 | run `33166210759` 单元测试已通过，Web typecheck 两处拒绝 `Uint8Array<ArrayBufferLike>` 作为 DOM BlobPart；测试 PNG helper 改为从明确 `ArrayBuffer` 构造 | 失败保留；仍为 `local-candidate`；构建和 Chrome 门未运行，等待重跑 |
| 2026-08-28 19:15 | PR #13 第二次 CI 全绿并合并 | FIX-BATCH-007 进入 main `873705b`；018 更新为 main | CI `33166313733` 通过 39 个 Vitest 文件/175 tests＋2 Node、8 workspace typecheck、生产构建、4 Chrome E2E 和 143 文件交底新鲜度；零用户图片/外联，非真实源验收 |
| 2026-08-31 15:17 | 用户授权自主推进至本机、GitHub、服务器三端最新可验收；启动 FIX-BATCH-008 | 新增 OMB-AUD-039，当前问题组总数 39、待修复 29；扩展七个已观察 QR 结构键且只保留键名 | `discovered → local-candidate`；零外联，待本地全门 |
| 2026-08-31 15:21 | FIX-BATCH-008 本地全门与公开样本回放通过 | 29 个已解码公开二维码全部通过新适配器；5 张仍为图像解码失败；全量测试、类型、构建与 4 条 Chrome E2E 通过 | `local-verified`；零上游图源请求，待 GitHub PR/CI |
| 2026-08-31 15:24 | PR #15 与 main CI 全绿 | FIX-BATCH-008 合并为 main `382e0e6`；OMB-AUD-039 更新为 main | PR CI `33368112824`、main CI `33368228496` 全绿；非真实源验收 |
| 2026-08-31 15:32 | FIX-BATCH-009 独立生产制品达到本地验证 | 首次 ESM bundle 因动态 require 失败，第二次独立 release 因 pngjs 外部依赖失败；均保留并改为静态 bundle。两次构建哈希一致，隔离 release 健康/鉴权/持久化/SIGTERM 通过 | OMB-AUD-006/027 `local-verified`；待公开 E2E 与 GitHub CI |
| 2026-08-31 15:35 | FIX-BATCH-009 本地全门通过 | 178 Vitest＋2 Node、8 workspace typecheck、build、production smoke、4 Chrome E2E、npm audit 0、169 文件交底新鲜度全绿 | OMB-AUD-006/027/031/035 `local-verified`；待 PR/CI |
| 2026-08-31 15:38 | PR #16 与 main CI 全绿 | FIX-BATCH-009 合并为 main `b40ed0d`；006/027/031/035 更新为 main | PR CI `33369111954`、main CI `33369227321` 全绿；非服务器部署/业务验收 |
| 2026-08-31 15:43 | 两台服务器只读盘点并启动 FIX-BATCH-010 | 选择腾讯机；目标目录和端口均为空，使用项目专用 Node runtime 与 loopback nginx/SSH tunnel | `discovered → local-candidate`；尚未写服务器 |
| 2026-08-31 15:53 | PR #17/main CI 后完成腾讯私有部署 | main `33f7f06` 安装为 versioned release；第一轮因宝塔 nginx 不 include `/etc/nginx/conf.d` 导致 8080 未监听，改用实际 include 后全门通过 | 应用制品 `deployed`；39 组问题仍为 15 main / 24 未闭合，真实 Ovi 与用户 accepted 未达 |
| 2026-08-31 16:00 | 部署证据 PR #18/main CI 与服务器回滚演练完成 | GitHub/main `62ab114`、CI `33370752935`；服务器前滚、回滚、再前滚并保持状态哈希 | FIX-BATCH-010 完成到 `deployed`；运行源码/制品哈希一致，证据文档提交不要求伪造新 runtime release；真实源与 24 组未闭合项不随之关闭 |
| 2026-08-31 17:37 | 容量恢复并启动 FIX-BATCH-011 | 增加严格非秘密日期/probe 配置、启动时单瓦片探测和按解码结果的 runtime ready 门 | `local-verified`：182 Vitest＋2 Node＋8 typecheck；未真实外联，OMB-AUD-002 仍 partial，待 PR CI |
| 2026-08-31 17:48 | PR #20 与 main CI 全绿 | FIX-BATCH-011 合并为 main `94e42b1`；PR/main CI 全门通过 | OMB-AUD-002 探针编排达到 main、整体仍 partial；本机跌至约 7.3 GiB，腾讯 runtime 更新暂停 |
| 2026-08-31 18:19 | 容量恢复并完成 FIX-BATCH-011 腾讯部署 | 本机 production build/smoke 通过；腾讯新增 release `94e42b1` 并原子切换 current，gateway/Web hash、health、双回环和持久状态 hash 通过 | OMB-AUD-002 的代码达到 deployed，整体仍 partial；未请求真实 Ovi 源，accepted 未达 |
| 2026-08-31 18:32 | 部署证据 PR #22/main CI 全绿 | deployment evidence commit `a92ff8c` 进入 main，CI `33382682547` 全绿 | docs-only evidence；runtime/current 仍为 `94e42b1`，真实源阶段不变 |
| 2026-08-31 18:51 | FIX-BATCH-012 本地全门通过 | 新增加密 vault、严格 bundle、同 UUID ref、API/Web/准备度真值；196 Vitest＋2 Node、8 typecheck、build/smoke、4 E2E | `local-verified`；待 PR CI/main/deploy，零真实凭证与外联 |
| 2026-08-31 19:08 | 容量门重新触发并发现 OMB-AUD-040 | 根卷约 7.15 GiB；一条命令只打印 df 后仍继续 build/smoke。新增所有 root 重型入口自动 `env:check` 与静态接线测试 | 误跑结果不作最终验证；分支回到 `local-candidate`，停止本机重任务并交由 GitHub CI |
